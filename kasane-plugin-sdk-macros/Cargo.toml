[package]
name = "kasane-plugin-sdk-macros"
version = "0.1.0"
edition = "2024"
description = "Proc macros for the Kasane WASM plugin SDK"
license = "MIT OR Apache-2.0"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
