[plugin]
id = "surface_probe"
abi_version = "1.0.0"

[authorities]
host = ["dynamic-surface"]

[handlers]
flags = ["overlay", "input-handler", "surface-provider", "contributor"]

[view]
deps = ["buffer-content", "buffer-cursor"]
