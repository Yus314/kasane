[plugin]
id = "smooth_scroll"
abi_version = "1.0.0"

[handlers]
flags = ["scroll-policy"]

[view]
deps = ["settings"]

[settings.enabled]
type = "bool"
default = false
description = "Enable smooth scrolling animation"
