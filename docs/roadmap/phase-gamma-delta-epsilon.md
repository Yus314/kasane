# Phase γ + δ + ε — Structural cascade

Per-phase closure notes for the structural cascade refactor program.
See `docs/roadmap.md` for active workstreams.

**Refactor program — Phase γ + δ + ε (structural cascade) — opened 2026-05-12.**
After Phase β-3.3 closed (`f98c2425`, β-3.3e) with `PluginBackend`
extinct, residual structural debt was surveyed across five
consideration rounds. **Remaining γ-1 findings** (post γ-1.1 / γ-1.2 /
γ-1.3 closure): 3-layer handler-dispatch redundancy (HandlerRegistry
77 setters → HandlerTable 55 erased types → PluginBridge 42 dispatch
sites, 4-place manual sync per new handler — scheduled for γ-3);
hot-path `PluginId(String)` clones and `Vec<Vec<Atom>>` allocations
(γ-4); dual `display/` + `display_algebra/` top-level dirs (γ-2.1);
and `#[doc(hidden)] pub` as a workaround for cross-crate internal
surface, 30+ consumer files (ε-1). Backward compatibility is
intentionally lifted. Total estimated wall-clock: **40-60 days**
across γ (structural cleanup), δ (design rethinking), ε
(workspace reorganization).

The original "dead architecture" finding (`render/pipeline.rs` +
its 752-LoC parity test) landed as Phase γ-1.1 (`6da05fa2`); the
`widget/tests.rs` shim deconstruction landed as Phase γ-1.2
(`96e67c9f`); the vestigial single-line absorbs (`manifest.rs`,
`perf.rs`) landed as Phase γ-1.3 (`0822ec94`).

Decisions taken at program open (2026-05-12, via interactive dialogue):

- **Scope**: full γ + δ + ε (workspace-level reorganization)
- **Order**: strict sequential (γ-0 → γ-1 → γ-2 → γ-3 → γ-4 → δ → ε)
- **WIT sharing** (γ-0.4): dedicated `kasane-wit` crate consumed
  by `kasane-wasm` / `kasane-plugin-sdk` / `kasane-plugin-sdk-macros`,
  replacing the current symlink scheme
- **Legacy pipeline** (γ-1.1): full deletion of `render/pipeline.rs`
  (878 LoC) and `tests/salsa_pipeline_comparison.rs` (752 LoC) —
  ADR-047 Salsa canonical makes the parity test load-bearing for
  nothing
- **Widget shim** (γ-1.2): all 22 `backend_*` tests migrate to
  `PluginRuntime` direct use; WidgetBackend shim deleted entirely
- **`plugin/` grouping** (γ-2.2): 3-way (`algebra/` pure value
  types, `host/` runtime context, `effect/` side effects) with
  9 files remaining at top level
- **`#[handler_table]` macro DSL** (γ-3.1): type-alias signature
  style (`handler init: Lifecycle<Effects>;`) — drives
  HandlerTable, dispatch methods, registry setters, and the
  `EXPECTED_HANDLER_NAMES` table from a single 22-entry spec module
- **Hot-path** (γ-4): all three items in scope —
  `PluginId(String) → Arc<str>` (γ-4.1), `Vec<Vec<Atom>>` scratch
  (γ-4.2), `dyn_clone` snapshot retired via δ-2 (γ-4.3 merged
  into δ-2)
- **Plugin trait** (δ-1): introduce `StatelessPlugin: Plugin<State = ()>`
  blanket for WASM/adapter plugins, formalizing the current idiom
- **PluginState ↔ Salsa input** (δ-2): generation counter +
  `dyn_clone` snapshot retired; plugin state lives as a Salsa
  input with revision-based invalidation
- **Examples** (δ-3): in-tree examples curated to **2**
  (`cursor-line`, `color-preview`); the remaining 9 examples
  (including the docs-referenced `sel-badge` / `prompt-highlight` /
  `smooth-scroll`) move to a future `kasane-plugin-gallery`
  external repo, and `docs/plugin-development.md` /
  `docs/plugin-cookbook.md` switch to external pointers
- **Error handling** (δ-4): `thiserror` enum at library
  boundaries, `anyhow` at binary boundary
- **Cargo features** (δ-5): `syntax` / `wasm` / `std-os` flags
  at `kasane-core` for slim builds
- **`kasane-internal` crate** (ε-1): absorbs `salsa_queries`,
  `salsa_views`, `display::algebra`, `WireFace`,
  `RecoveryWitness`, `SafeDisplayDirective`; `#[doc(hidden)] pub`
  retired
- **`kasane-protocol` crate** (ε-2): Kakoune JSON-RPC moves out
  of `kasane-core`
- **`kasane-core-tests` crate** (ε-3): the 20754-LoC integration
  test surface isolated, enabling fast `cargo test --lib`
- **ADR per-file split** (ε-4): `docs/decisions.md` expands into
  `docs/decisions/adr-NNN-*.md`

Bench baseline `gamma` captured at program open
(`cargo bench --bench rendering_pipeline -- --save-baseline gamma`);
all subsequent commits compare against this baseline with a ±5%
frame-time gate (G4) and a <1 % iai instruction-count gate (G5).

| Phase | Status | Notes |
|---|---|---|
| γ-0 — Verification infrastructure | ✅ 2026-05-12 | All four sub-stages landed (`ff4a8b7f` / `3c687198` / `6657a340` / `177aff80`) |
| γ-0.1 — `tools/check-doc-consistency.sh` expansion | ✅ 2026-05-12 (`ff4a8b7f`) | 4 new validators added under `--full` (CI) mode: `check_claude_md_workspace` (every backticked Workspace Structure path exists as directory), `check_claude_md_modules` (every absolute Key Module Locations path exists), `check_roadmap_pending_unique` (no duplicate `pending` rows), `check_decisions_adr_refs` (every ADR-NNN reference resolves). `--quick` (pre-commit) mode unchanged. |
| γ-0.2 — CLAUDE.md / roadmap drift fix | ✅ 2026-05-12 (`3c687198`) | Resolved every error flagged by the γ-0.1 expansion: removed `kasane-plugin-model/` ghost row from CLAUDE.md Workspace Structure (the directory does not exist; types live in kasane-core); deduplicated β-3.3b.12 pending row; pruned α-3-deleted lifecycle scaffolding from Deferred list; updated PluginBackend proc-macro note to point at γ-3 (`#[handler_table]` DSL). |
| γ-0.3 — tree-sitter `unsafe impl` SAFETY annotation | ✅ 2026-05-12 (`6657a340`) | `kasane-syntax/src/provider.rs:27-28` SAFETY blocks split into Send and Sync paragraphs naming the `Arc::get_mut` discipline in `SyntaxManager::update` as the exclusivity gate. Replaces the prior "we only access them from a single thread" hand-wave with a verifiable claim. |
| γ-0.4 — `kasane-wit` crate extraction | ✅ 2026-05-12 (`177aff80`) | New `kasane-wit/` workspace member owns `wit/plugin.wit`. Previous canonical copy at `kasane-wasm/wit/plugin.wit` plus two symlinks (`kasane-plugin-sdk/wit/plugin.wit`, `kasane-plugin-sdk-macros/wit/plugin.wit`) deleted. Three consumers retargeted via Cargo dependency: `kasane-wasm` (`wasmtime::component::bindgen!` path → `../kasane-wit/wit`; `abi.rs` `include_str!` → `kasane_wit::WIT`); SDK + SDK-macros similarly. CI WIT symlink check replaced by content hash check. |
| γ-1 — Dead architecture purge | ✅ 2026-05-12 | All four sub-stages landed (`6da05fa2` / `96e67c9f` / `0822ec94` / `d961b859`); γ-1.1 (`render/pipeline.rs` + parity test deletion) was the LoC-dominant move (-2079 LoC). |
| γ-1.1 — `render/pipeline.rs` + `salsa_pipeline_comparison.rs` deletion | ✅ 2026-05-12 | Deleted `kasane-core/src/render/pipeline.rs` (-878 LoC), `kasane-core/src/render/tests/pipeline.rs` (-449 LoC), `kasane-core/tests/salsa_pipeline_comparison.rs` (-752 LoC). Shared core (`PreparedFrame`, `prepare_frame`, `render_cached_core`, `scene_render_core`, `populate_inline_box_paint_commands`, private helpers, `inline_box_dispatch_tests`) absorbed into `pipeline_salsa.rs`; `ViewSource` trait + `DirectViewSource` deleted (single-impl trait collapsed to direct `SalsaViewSource` calls). Eight consumers migrated to `render_pipeline_cached` / `scene_render_pipeline_cached`: `test_support.rs` (`render_to_grid`, `render_to_grid_with_result`), `src/render/tests/{scene_cache,cursor_position}.rs`, `tests/{trace_equivalence,rendering_pipeline}.rs`, `benches/{replay,rendering_pipeline}.rs`, `kasane-tui/benches/backend.rs`, `kasane-gui/benches/cpu_rendering.rs`. The salsa-vs-legacy paired benches in `bench_salsa_vs_legacy` (renamed `bench_salsa_full`) lost their `*_legacy` halves. Doc sweep: `decisions.md` ADR-016 marked Superseded, ADR-047 §Decision rewritten with γ-1.1 closure note; `semantics.md` A1/T1/T3 rewritten for Salsa-only canonical path; `performance.md` `scene_render_pipeline` → `scene_render_pipeline_cached`. |
| γ-1.2 — `widget/tests.rs` shim deconstruction | ✅ 2026-05-12 | −74 LoC net (file 2348 → 2274). The 172-LoC `WidgetBackend` struct + its 11 impl methods deleted; replaced with 6 thin free functions (`widgets_runtime`, `widgets_reload`, `parse_failure_diagnostic`, `aggregate_capabilities`, `aggregate_view_deps`, `first_capability_descriptor`, `contribute_aggregated`) totalling ~100 LoC. All 22 `backend_*` tests rewritten against `PluginRuntime` + the existing `first_decorate_background_for_test` / `first_decorate_gutter_for_test` / `first_transform_patch_for_test` / `all_slot_*_for_test` helpers. The legacy `state_hash`/`drain_diagnostics`/`id` test-shim concepts retired: `backend_id` deleted (the legacy single-plugin id "kasane.widgets" no longer fits the multi-WidgetPlugin runtime); the three reload-generation tests collapse to two reload-only tests since the `state_hash` shim's monotonic counter is no longer modeled. The roadmap's -1500 LoC estimate was an overestimate — `widget/tests.rs` is dominated by test bodies that survive the migration, not the shim. |
| γ-1.3 — Vestigial single-line absorbs | ✅ 2026-05-12 | −52 LoC net. `kasane-wasm/src/manifest.rs` deleted; the `pub use kasane_plugin_package::manifest;` re-export moves into `kasane-wasm/src/lib.rs` (one line) so `crate::manifest::PluginManifest` access path stays intact for the four `tests/` consumers. `kasane-core/src/perf.rs` deleted (the cfg-gated `perf_span!` macro stub) together with the `mod perf;` line in `kasane-core/src/lib.rs`, the `perf-tracing` feature in `kasane-core/Cargo.toml`, ten `crate::perf::perf_span!(…)` call sites across `render/{view/mod,grid,paint,pipeline_salsa}.rs` + `layout/flex.rs` + `plugin/registry/mod.rs`, and the `## Runtime Tracing with perf-tracing` section in `docs/profiling.md`. Span instrumentation was only ever wired through this macro — the feature is replaceable with ad-hoc `tracing::info_span!` insertions if needed. |
| γ-1.4 — Deferred-list cleanup | ✅ 2026-05-12 | The "Findings:" paragraph at the program-open header rewritten to list only **remaining** γ scope (γ-2.1 / γ-3 / γ-4 / ε-1 items) — the legacy `render/pipeline.rs` + parity test, the `widget/tests.rs` shim, and the vestigial single-line absorbs items moved to a brief "landed as" closure note pointing at `6da05fa2` / `96e67c9f` / `0822ec94`. The orphan "verification log" bullet at the bottom of the Deferred-ADR list (a historical 9-candidate analysis from program open) deleted — the verify-before-cut discipline is now operational discipline embodied in γ-1.x execution, not a load-bearing list entry. |
| γ-2 — Structural reorganization | ✅ 2026-05-13 | All three sub-stages landed (`d9189d7c` γ-2.1 / `92f7bffe` γ-2.2 / `201eb803` γ-2.3); LoC-neutral. |
| γ-2.1 — `display_algebra/` → `display/algebra/` absorption | ✅ 2026-05-12 | `git mv kasane-core/src/display_algebra kasane-core/src/display/algebra` (history-preserving move, 9 files + 2-file bridge/ subdir). Inside the moved tree, `bridge.rs` + `bridge/` renamed to `runtime_bridge.rs` + `runtime_bridge/` to free the `bridge.rs` name for γ-2.3's `plugin/plugin_bridge.rs` rename. `kasane-core/src/lib.rs` lost its `pub mod display_algebra;` declaration; `kasane-core/src/display/mod.rs` gained `pub mod algebra;`. `kasane-core/src/display/algebra/mod.rs` `pub mod bridge` → `pub mod runtime_bridge`. Eight Rust files updated via bulk `crate::display_algebra::bridge::` → `crate::display::algebra::runtime_bridge::` and `crate::display_algebra::` → `crate::display::algebra::` rewrite (`plugin/registry/collection/display.rs`, `display/{resolve,tests,unit}.rs`, `display/algebra/apply.rs`, `salsa_queries.rs`, `benches/{bridge_overhead,iai_pipeline}.rs`, file-internal doc-comments in `runtime_bridge.rs`). The ADR-036 reserved-slot citations in `docs/decisions.md` updated to the new paths. LoC-neutral. |
| γ-2.2 — `plugin/` subdir reorganization | ✅ 2026-05-13 | Three new subdirs landed: `plugin/algebra/` (4 files — `element_patch`, `compose`, `safe_directive`, `recovery_witness`), `plugin/host/` (4 files — `app_view`, `context`, `variable_store`, `setting`), `plugin/effect/` (6 files — `effects`, `effect_tiers`, `command`, `error_attribution`, `kakoune_transparent_command`, `kakoune_transparent_effects`). 14 files git-mv'd preserving history; `plugin/mod.rs` `pub mod` declarations replace the 14 top-level ones with `pub mod {algebra,host,effect};`, and the 14 corresponding `pub use FOO::{…}` re-exports retarget through the subdirs (so external `kasane_core::plugin::AppView` paths remain valid). The originally-listed `predicate` algebra member has no source today and is omitted. Caller path rewrite swept 55 files: `crate::plugin::FOO::` → `crate::plugin::<subdir>::FOO::`, `kasane_core::plugin::FOO::` → `kasane_core::plugin::<subdir>::FOO::`, and the relative `super::FOO::` / `super::super::FOO::` patterns inside `plugin/` were retargeted per file location. LoC-neutral (155 insertions / 142 deletions, +13 net for the new `mod.rs` headers). |
| γ-2.3 — `plugin/bridge.rs` → `plugin/plugin_bridge.rs` | ✅ 2026-05-13 | Single `git mv kasane-core/src/plugin/bridge.rs kasane-core/src/plugin/plugin_bridge.rs` preserves the 4257-LoC file's history. `plugin/mod.rs` `pub mod bridge;` → `pub mod plugin_bridge;` and `pub use bridge::PluginBridge;` → `pub use plugin_bridge::PluginBridge;`. Three internal callers retargeted: `plugin/registry/mod.rs::use super::bridge::PluginBridge` → `super::plugin_bridge::PluginBridge`; `plugin/state.rs` doc-link `(super::bridge::PluginBridge)` → `(super::plugin_bridge::PluginBridge)`; `widget/plugin.rs::use crate::plugin::{…, bridge::PluginBridge}` → `plugin_bridge::PluginBridge`. The cross-module name `bridge.rs` was freed by γ-2.1's `display_algebra/bridge.rs` → `display/algebra/runtime_bridge.rs` rename; γ-2.3 retires the last in-repo `bridge.rs` so future readers of `display/algebra/runtime_bridge.rs` no longer have to disambiguate. |
| γ-3 — proc-macro auto-generation | ✅ 2026-05-13 | All sub-stages landed: γ-3.1 (spec doc), γ-3.2 (`kasane-macros::handler_table` impl, 13 sub-stages), γ-3.3 (manual code retirement, 5+ sub-stages). 4-place-sync rule (`.claude/rules/plugin-handlers.md`) deleted alongside `exhaustive_handler_dispatch_coverage` test. The plugin handler subsystem is now structurally macro-derived; carve-outs (`define_projection`, `on_key_map`, `on_state_changed_for`, `on_transform_full`, `on_process_task_*`) are documented exceptions per spec §9. |
| γ-3.1 — `#[handler_table]` DSL spec | ✅ 2026-05-13 | Spec landed at `docs/handler-table-dsl.md`. Type-alias signature style (`handler init(app: &AppView<'_>): Lifecycle<Effects>(tier1, transparent);`); 70 canonical entries (17 Lifecycle / 8 Observer / 5 Dispatcher / 28 View / 12 Config) — the original "22" estimate undercounted by ~3× due to pre-ADR-044 / pre-ADR-035 / pre-ADR-031-Phase-10 baseline. 4 dispatch shapes formalised (`Lifecycle<E>` / `Observer` / `Dispatcher<C>` / `View<Out>` with `PerSlot=K` / `Prioritized` / `Unified` modifiers). 6 generated artefacts per entry enumerated (erased alias, `HandlerTable` field, `empty()` line, registry setter pair incl. tier / transparent / safe / witnessed variants, bridge dispatch method, `EXPECTED_HANDLER_NAMES` const slot). 3 carve-outs documented (`define_projection` bespoke surface, `on_key_map` orchestrator, `on_state_changed_for` filter sugar). Migration discipline: γ-3.2 ships parallel macro output gated by type-equality + name-set-equality assertions; γ-3.3 deletes manual code in defined order and removes `.claude/rules/plugin-handlers.md` 4-place-sync rule in the same commit as the `exhaustive_handler_dispatch_coverage` test. Open questions (spec module location, doc-comment forwarding, `state`/`app` arg-name reservation) deferred to γ-3.2. |
| γ-3.2 — `kasane-macros::handler_table` implementation | ✅ 2026-05-13 | Closed in 13 sub-stages over one day: γ-3.2.1 (parser + base codegen); γ-3.2.2a–e (HandlerRegistry + 13 modifiers across 6 increments); γ-3.2.3a–c (kasane-core integration + 68-entry spec module + 4 type-equality assertions). Total: `kasane-macros/src/handler_table.rs` 1616 LoC + `kasane-core/src/plugin/handler_table_spec.rs` 380 LoC + 9 trybuild tests in kasane-macros + 5 unit tests in kasane-core. Parallel-impl gate landed: macro-generated `HandlerTable` is the canonical superset, manual ⊆ generated assertion holds, type equality verified per shape. γ-3.3 (manual code deletion) is now unblocked. |
| γ-3.2.1 — macro skeleton + base-shape data codegen | ✅ 2026-05-13 | `kasane-macros/src/handler_table.rs` (+390 LoC). Function-like macro (`handler_table!{}` not `#[handler_table]` — attribute macros require valid Rust body, custom `handler …;` keyword needs raw-token entry). Parser covers full §5.2 grammar incl. all 13 modifiers; codegen emits erased type aliases + `HandlerTable` struct + `empty()` constructor + `EXPECTED_HANDLER_NAMES` const for the four base shapes. Modifiers parse but are gated with a γ-3.2.2 hint at codegen. trybuild: 1 pass test + 3 fail tests (`handler_table_bad_shape` / `handler_table_modifier_on_wrong_shape` / `handler_table_reserved_arg_name`). Reserved-name validation rejects `state` (implicit first arg) as both handler-name and arg-name. `lib.rs` gains `pub fn handler_table` proc-macro export. |
| γ-3.2.2a — HandlerRegistry + base-shape setters | ✅ 2026-05-13 | `handler_table.rs` +130 LoC (560→690 total). Generates `pub(crate) struct HandlerRegistry<S: PluginState + Clone + 'static>` with `new()` / `into_table()` and one `pub fn on_<name>` setter per handler entry. Setter bodies match the existing `register_state_effect!` / `register_state_only!` / `register_view!` template (downcast `&S`, call user closure, box new state). Lifecycle / Observer / Dispatcher / View shapes each get their own template. `arg_idents_and_types` validates arg patterns up front (rejects destructuring) so a bad arg surfaces as a single-line error rather than cascading "PluginState not in scope" downstream. Pass test now exercises the full register-then-into_table round-trip with a `u32`-state plugin. New fail test `handler_table_destructure_arg` covers the new validator path. |
| γ-3.2.2b — Lifecycle tier / transparent setter variants | ✅ 2026-05-13 | `handler_table.rs` 724 → 921 LoC (+197). Additive setter model: `tier1` modifier on Lifecycle emits `on_<name>_tier1<E: Into<KakouneSideEffects>>` alongside the base; `tier2` mirrors with `ProcessCapableEffects`. `transparent` modifier on Lifecycle replaces the base setter with one bound `<E: Into<Effects> + Transparency>` that flips a `TransparencyFlags` bit on registration when `E::IS_TRANSPARENT`. `TransparencyFlags` struct + `is_all_lifecycle_transparent(&table)` predicate generated from the union of `transparent`-marked Lifecycle entries; `transparency: TransparencyFlags` field added to `HandlerTable` unconditionally (zero-byte when empty). Tier wrappers double-`Into`-lift through the tier type to `Effects` at the table boundary, matching the manual `on_init_tier1` / `on_io_event_tier2` pattern. New pass test `handler_table_tier_transparent` exercises all three modifiers + the transparency-flag round-trip; new fail test `handler_table_tier1_rejects_effects` confirms tier1's `Into<KakouneSideEffects>` bound rejects raw `Effects` returns at compile time (no `From<Effects> for KakouneSideEffects` impl exists). Dispatcher transparent (signature involves a generic-Vec rewrite) deferred to γ-3.2.2c. |
| γ-3.2.2c — View per_slot / prioritized storage | ✅ 2026-05-13 | `handler_table.rs` 921 → 1056 LoC (+135). `per_slot=K` modifier swaps the `Option<ErasedHandler>` field for `Vec<<Name>Entry>` (using the plural `_handlers` suffix matching `contribute_handlers` / `gutter_handlers`); the auto-generated `<Name>Entry` struct carries `key: K` plus `handler: Erased<Name>Handler`. `prioritized` adds `priority: i16` to the entry struct and threads it through the setter signature. Combined: gutter pattern `(per_slot = GutterSide, prioritized)` produces `pub fn on_gutter(&mut self, key: GutterSide, priority: i16, handler: F)` that pushes one entry per call. New pass test `handler_table_per_slot` exercises both `contribute` (per_slot only, two STATUS_LEFT/STATUS_RIGHT registrations) and `gutter` (per_slot + prioritized, two GutterSide×priority registrations). Currently restricted to View shape; Observer per_slot (subscribe per-topic handler) deferred to γ-3.2.2d. `void` modifier (Observer with no state return) and Dispatcher transparent (generic-Vec rewrite) also deferred to γ-3.2.2d. |
| γ-3.2.2d-1 — Observer per_slot + void | ✅ 2026-05-13 | `handler_table.rs` 1056 → 1108 LoC (+52). `void` modifier on Observer drops the state-return: erased alias becomes `Box<dyn Fn(&dyn PluginState, args...) + Send + Sync>`, setter accepts `Fn(&S, args...)` (no return), wrapper does not box. `per_slot=K` extended to Observer (subscribe per-topic per-value handler pattern) — same `Vec<<Name>Entry>` storage as View per_slot but the wrapper boxes the handler's `S` return into the type-erased `Box<dyn PluginState>` slot. Pass test `handler_table_observer_extras` exercises both: `shutdown(): Observer(void)` and `subscribe(_value: &ChannelValue): Observer(per_slot = TopicId)` registered with two distinct topics. |
| γ-3.2.2d-2 — View unified + suppresses | ✅ 2026-05-13 | `handler_table.rs` 1108 → 1167 LoC (+59). Pure metadata generation — no field/setter changes. `unified` modifier emits `pub(crate) fn has_<name>(&self) -> bool` on `HandlerTable` returning true iff the unified handler is registered (matches the manual `has_decomposed_annotations` inverse predicate at `plugin_bridge.rs:729`). `suppresses=[a, b, c]` emits `pub(crate) const SUPPRESSED_BY_<NAME>: &[&str] = &["a", "b", "c"]` listing the decomposed handler names superseded when the unified entry is registered (the bridge reads this to skip the listed handlers in its dispatch loop). Pass test `handler_table_unified` registers the unified `annotate_line` handler and confirms the predicate flips + the const carries `["gutter", "background", "inline"]`. |
| γ-3.2.2d-3 — View recovery (display family) | ✅ 2026-05-13 | `handler_table.rs` 1167 → 1331 LoC (+164). `recovery` modifier on View emits the trio of setters: base `on_<name>` flips status to `Unwitnessed` after registration; `on_<name>_safe<F>` accepts closures returning `Vec<SafeDisplayDirective>`, the wrapper lifts each via `Into::into` to `DisplayDirective` and stores in the existing `Vec<DisplayDirective>` slot, status flips to `NonDestructive`; `on_<name>_witnessed(witness, handler)` takes a `RecoveryWitness` arg and stores it inline as `Witnessed(witness)`. Per-recovery-entry field `<name>_recovery: DisplayRecoveryStatus` added to `HandlerTable` (default `NotRegistered`). The macro generates a local `pub(crate) enum DisplayRecoveryStatus` (with `Default` impl) inside the spec module — only when at least one entry uses `recovery` — mirroring the manual `pub(crate)` enum in `kasane-core` (which is unreachable from external spec modules). γ-3.2.3's parallel-impl gate will assert structural equivalence. Pass test `handler_table_recovery` exercises all four states (NotRegistered → Unwitnessed → NonDestructive → Witnessed). |
| γ-3.2.2d-4 — Dispatcher transparent | ✅ 2026-05-13 | `handler_table.rs` 1331 → 1421 LoC (+90). Generic-Vec rewrite: the macro detects `transparent` on Dispatcher and parses the spec entry's command type as `Vec<T>` via the new `extract_vec_inner(ty)` helper (returns `compile_error` if the spec lists a non-Vec command type, since the rewrite has no inner element to generic-substitute). The setter signature becomes `pub fn on_<name><F, C>(&mut self, handler: F)` with bound `C: Into<T> + Transparency`; the wrapper does `cmds.into_iter().map(Into::into).collect()` to lift `Vec<C>` back to `Vec<T>` at the table boundary. `TransparencyFlags.<entry>` flips on registration when `C::IS_TRANSPARENT`. The previous Lifecycle-only `is_all_lifecycle_transparent` predicate is now joined by `is_all_input_transparent` (Dispatcher entries) and `is_fully_transparent` (the conjunction), matching the manual `TransparencyFlags::is_*` API at `handler_table.rs:469-497`. Pass test `handler_table_dispatcher_transparent` registers a `key` handler returning `Vec<KakouneTransparentCommand>` and asserts the flag flips + all three predicates return true. |
| γ-3.2.2e-1 — View default + stateless | ✅ 2026-05-13 | `handler_table.rs` 1421 → 1483 LoC (+62). `default = expr` modifier emits `pub(crate) fn default_<name>() -> <Out> { <expr> }` (a function rather than a const so non-const expressions like `vec![]` / `OrnamentBatch::default()` work) — the bridge calls this when no handler is registered. `stateless` modifier drops the implicit `&S` arg from both the erased alias and the setter signature; the wrapper does not downcast (matches the lenses-factory `Fn() -> Vec<Arc<dyn Lens>>` pattern). Pass test `handler_table_default_stateless` exercises both: `virtual_text` with `default = vec![]` (regular &S arg + default helper), `lenses` with `(stateless, default = vec![])` (no &S arg, registered closure with zero parameters). |
| γ-3.2.2e-2 — transform metadata-storage + full_fallback carve-out | ✅ 2026-05-13 | `handler_table.rs` 1483 → 1616 LoC (+133). Third storage shape introduced: `Option<<Name>Entry>` (single-entry with metadata fields). Triggered by `prioritized` and/or `targets=T` on View *without* `per_slot`. The entry struct gains optional `priority: i16` / `targets: T` fields alongside the handler; the setter signature is `pub fn on_<name>(&mut self, [priority: i16,] [targets: T,] handler: F)` (positional, ordered priority → targets → handler). New helper `targets_type` extracts the type from the `targets=…` modifier; new predicate `has_metadata_storage` selects the new storage shape. `full_fallback` is rejected at codegen with a carve-out diagnostic pointing at the spec doc §9 — the on_<name>_full setter mentions `TransformSubject` (a one-off type that does not generalize), so γ-3.3 will hand-write the companion setter alongside the macro-generated members. The macro generates the `full_handler: Option<…>` companion *field* on the entry struct so the carve-out setter has a slot to populate (NB: γ-3.2.2e-2 codegen does not emit this field yet — it lands when γ-3.3 wires up transform). Pass test `handler_table_metadata` exercises the prioritized+targets storage with a `transform`-shaped entry; new fail test `handler_table_full_fallback_carve_out` confirms the carve-out diagnostic. **γ-3.2.2 fully closed.** |
| γ-3.2.3a — kasane-core integration scaffold | ✅ 2026-05-13 | `kasane-core/src/plugin/handler_table_spec.rs` (+115 LoC, +1 line in `plugin/mod.rs`). The `kasane-macros` dep is already wired into `kasane-core`; the new `pub(crate) mod handler_table_spec` invokes `handler_table!{}` to generate a parallel `HandlerTable` / `HandlerRegistry<S>` / `EXPECTED_HANDLER_NAMES` under `plugin::handler_table_spec::generated::*`. Bootstrap covers 3 representative entries (init / observe_key / shutdown — one per shape category). Two unit tests: `generated_names_are_subset_of_manual_dispatch_coverage` asserts every macro-generated name appears in the manual `EXPECTED_HANDLER_NAMES` (the manual list is reproduced inline because it lives inside a `#[test] fn` body and is not module-visible); `empty_table_compiles_and_has_correct_entry_count` confirms the macro-generated `HandlerTable::empty()` round-trips. The subset assertion strengthens to equality (parallel-impl gate proper) once γ-3.2.3b lands the full 70-entry spec. |
| γ-3.2.3b-1/-2/-3 — Lifecycle + Observer + Dispatcher + View entries | ✅ 2026-05-13 | `handler_table_spec.rs` 115 → 244 LoC (+129); 58 entries declared (17 Lifecycle, 8 Observer, 5 Dispatcher, 28 View). `plugin_bridge.rs` `EXPECTED_HANDLER_NAMES` updated: `"on_subscription"` → `"subscription"` for naming convention consistency (every other entry omits the `on_` prefix). Assertion direction reversed to **manual ⊆ generated** — the spec module is the canonical superset; the manual coverage test under-covers `action` / `process_task` / `navigation_action` / `virtual_edit` (γ-3.1 §5 noted gap). `manual_dispatch_coverage_is_subset_of_generated` test now passes for all 36 manual entries against the 58-entry generated spec. `transparent_lifecycle_sets_flag_on_registration` test confirms macro-generated `on_command_error` round-trips `KakouneTransparentEffects` → `TransparencyFlags.command_error = true`. Existing `exhaustive_handler_dispatch_coverage` test continues to pass after the rename. |
| γ-3.2.3b-4 — Config entries (10) | ✅ 2026-05-13 | `handler_table_spec.rs` 244 → 352 LoC (+108). 10 config entries declared (interests / authorities / allows_process_spawn / display_priority / workspace_request / capabilities_override / capability_descriptor_override / state_hash / suppressed_builtins / key_map). The auto-generated `transparency` / `recovery` config sub-entries from spec §8.6 are macro-emitted from the `transparent` / `recovery` modifiers and not user-declared. New test `config_defaults_match_spec` asserts every config field in the generated `HandlerTable::empty()` matches its declared default (including `Default::default()` fall-through for `Option`-typed entries without an explicit default). Config entries do not appear in `EXPECTED_HANDLER_NAMES`; the manual ⊆ generated assertion is unaffected. **γ-3.2.3b complete — 68 spec entries declared (58 handlers + 10 config).** |
| γ-3.2.3c — type-equality assertion | ✅ 2026-05-13 | `kasane-core/Cargo.toml` +1 dev-dep (`static_assertions = "1"`); `handler_table_spec.rs` +30 LoC. New test `erased_aliases_type_equal_per_shape` calls `static_assertions::assert_type_eq_all!` for one representative `Erased<Name>Handler` alias per shape (Lifecycle: `init`; Observer: `workspace_changed`; Dispatcher: `key`; View: `overlay`). All four assertions pass on first try, confirming the macro's emitted erased fn signatures (using `::core::ops::Fn` / `::std::boxed::Box` absolute paths) resolve to the same compiler types as the manual aliases (which use prelude-resolved `Fn` / `Box`). Field-level struct equality between `HandlerTable` and `generated::HandlerTable` is intentionally NOT asserted: manual naming (`subscribers: Vec<SubscribeEntry>` keyed `topic`) and macro-generated naming (`subscribe_handlers: Vec<SubscribeEntry>` keyed `key`) diverge by design — γ-3.3 absorbs the alignment by deleting the manual side and renaming `plugin_bridge.rs` dispatch sites to consume the generated names. **γ-3.2 fully closed.** |
| γ-3.3 — Replace existing manual code | ✅ 2026-05-13 (γ-3.3a/b/c/d/e all closed) | All sub-stages landed: γ-3.3a-1/a-2 (alias-name alignment + type-eq expansion to 54); γ-3.3b-1 (top-level field-name alignment); γ-3.3b-2 (per_slot Entry internal field + struct renames); γ-3.3b-3 (recovery split into per-name fields, `RecoveryFlags` retired); γ-3.3b-4 (`process_task` carved out); γ-3.3c-1 (decoration setter-name alignment); γ-3.3c-2 (render + input setter-name alignment); γ-3.3c-3 (per_slot+recovery storage moves to per-entry struct); γ-3.3c-4a (`TransformEntry.full_handler` lifted to top-level); γ-3.3c-4b (entry-struct re-exports); γ-3.3c-4c (manual `HandlerTable` wrapper-struct refactor); γ-3.3c-5a (manual `HandlerRegistry` wrapper-struct refactor); γ-3.3c-5b (delete redundant manual setter modules); γ-3.3d (collapse `handler_table.rs` body); γ-3.3e (retire `exhaustive_handler_dispatch_coverage` test; remove `.claude/rules/plugin-handlers.md`). **γ-3 (handler-table DSL) fully closed** — γ-3.1 spec, γ-3.2 macro implementation, γ-3.3 manual-code retirement complete. The handler subsystem is now structurally macro-derived; carve-outs (define_projection, on_key_map, on_state_changed_for, on_transform_full, on_process_task_*) are the documented exception per spec §9. |
| γ-3.3e — retire dispatch coverage test + 4-place sync rule | ✅ 2026-05-13 | Deleted `exhaustive_handler_dispatch_coverage` test from `plugin_bridge.rs` (lines 1583-2046, ~463 LoC including the 36-entry `EXPECTED_HANDLER_NAMES` const, the `AllHandlersPlugin` test fixture that registered every handler, and the per-handler invocation/assertion logic). The test guarded the silent-failure window where adding a `HandlerTable` field without a corresponding `PluginBridge` dispatch would silently no-op; γ-3.2/γ-3.3's structural changes close most of that window — the macro now emits the field from a spec entry and the bridge dispatches against the field name (so a missing dispatch surfaces during macro generation rather than at runtime). The remaining silent-failure surface (adding a spec entry without a corresponding bridge dispatch site) is small and bounded by the spec module being the explicit single source of truth. Deleted `.claude/rules/plugin-handlers.md` (the 4-place-sync rule the test underwrote — handler_table.rs / handler_registry/ / plugin_bridge.rs dispatch / EXPECTED_HANDLER_NAMES — collapses to a 2-place sync after γ-3.3: spec entry + bridge dispatch). 1928 lib tests pass (-1 from retired test); workspace build + clippy + fmt clean. **γ-3.3 fully closed** — kasane-core/src/plugin manual handler subsystem is irreducible: handler_table.rs (396 LoC: GutterSide enum, ErasedFullTransformHandler carve-out alias, six re-exports from generated, HandlerTable wrapper struct + helper methods, capability inference, tests) + handler_registry/*.rs (2286 LoC: carve-out setters, tier-narrowing setters, transparency-check accessors, KeyMapBuilder, tests) + plugin_bridge.rs dispatch methods. |
| γ-3.3d — collapse `handler_table.rs` body | ✅ 2026-05-13 | Stripped `handler_table.rs` to its irreducible core: 788 → 396 LoC (~50% reduction). The ~50 manual `Erased<Name>Handler` type aliases retained for the now-retired `erased_aliases_type_equal_per_shape` parallel-impl test (γ-3.3c-4c) had no remaining in-tree consumers — every setter site in `handler_registry/*.rs` and dispatch site in `plugin_bridge.rs` resolves to `gen::Erased<Name>Handler` via the entry-struct re-exports (γ-3.3c-4b) and the `HandlerTable` Deref (γ-3.3c-4c). Only `ErasedFullTransformHandler` (`full_fallback` carve-out, spec §9.4) remains because the macro deliberately rejects the modifier and the manual setter `on_transform_full` references it. The file now contains: `GutterSide` pub enum (the one type plugin authors directly construct), the `ErasedFullTransformHandler` carve-out alias, six re-exports from generated (`ContributeEntry` / `GutterEntry` / `ProjectionEntry` / `TransformEntry` / `DisplayRecoveryStatus` / `TransparencyFlags`), the `is_visually_faithful` free fn (orphan-rule workaround for adding a method to a re-exported foreign enum), and the `HandlerTable` wrapper struct + Deref/DerefMut + impl block (`empty()` / `is_*_transparent()` / `capabilities()` / `interests()` / `has_annotation_handlers()` / `capability_descriptor()`). 1929 lib tests pass; workspace build + clippy + fmt clean. **Surface area reduced**: the manual side of the plugin handler subsystem is now `handler_table.rs` (396 LoC, mostly the `capabilities()`/`capability_descriptor()` inference and tests) + `handler_registry/*.rs` (2286 LoC, mostly carve-outs + tier-narrowing setters + the test module). γ-3.3e completes the cleanup by retiring the `exhaustive_handler_dispatch_coverage` test and `.claude/rules/plugin-handlers.md`. |
| γ-3.3c-5b — delete redundant manual setter modules | ✅ 2026-05-13 | Stripped each axis module in `handler_registry/*.rs` to its carve-out core, deleting all setter methods that have generated counterparts. Plugin code now invokes the generated setters via `Deref` from `HandlerRegistry` to `gen::HandlerRegistry<S>`. Files reduced (LoC): `decoration.rs` 318 → 87 (kept `is_display_recoverable` query + `define_projection` + `define_additive_projection` carve-outs §9.1); `extension.rs` 263 → 172 (kept `publish` / `publish_raw` / `subscribe` / `subscribe_raw` / `publish_typed` / `subscribe_typed` typed serialization wrappers); `input.rs` 890 → 622 (kept `_tier1` narrowing setters — Dispatcher entries don't get tier modifiers in the spec — plus `is_input_transparent` / `is_lifecycle_transparent` / `is_fully_transparent` accessors and the tier-1 mouse/key/text-input integration test module); `lifecycle.rs` 474 → 190 (kept `declare_*` config setters — macro doesn't auto-emit setters for `config` entries — plus `on_process_task_tier2` / `on_process_task_streaming_tier2` carve-outs §9.5); `render.rs` 184 → 106 (kept `on_key_map` orchestrator §9.2 + `declare_key_map` config write); `transform.rs` 225 → 110 (kept `on_transform(priority, handler)` no-targets convenience + `on_transform_for(priority, &targets, handler)` slice adapter + `on_transform_full` carve-out §9.4); `mod.rs` 383 → 388 (retired the four `register_state_*` / `register_view` / `register_void` helper macros that the deleted setters used). Total LoC: handler_registry/ 3348 → 2286 (-1062, ~32% reduction); the 611 LoC `tests.rs` is unchanged because it exercises the public API through the same setter names — `r.on_init_tier1(...)` etc. now resolves to generated via Deref. **Cleanup**: `ErasedSurfacesHandler` and `ErasedDisplayHandler` aliases marked `#[allow(dead_code)]` (consumed only by the carve-out setters that delegate to generated `on_<name>`); 4 unused boxing-helper macros deleted; orphan doc-comments above retired-setter placeholder comments stripped. All 1929 lib tests pass; workspace build + clippy + fmt clean. **The plugin handler API surface is now mostly macro-emitted**; γ-3.3d will collapse the manual `handler_table.rs` body (still ~990 LoC of erased aliases referenced only by the parallel-impl tests + the `HandlerTable` wrapper definition itself) once the remaining alias references stabilize. |
| γ-3.3c-5a — `HandlerRegistry` wrapper-struct refactor | ✅ 2026-05-13 | The manual `HandlerRegistry<S>` is now a thin wrapper around the macro-generated `gen::HandlerRegistry<S>` plus carve-out registration state: `{ inner: gen::HandlerRegistry<S>, process_tasks: Vec<ProcessTaskEntry>, transform_full_handler: Option<ErasedFullTransformHandler> }`. `Deref`/`DerefMut` to `gen::HandlerRegistry<S>` make `r.on_init(...)` etc. transparently invoke the generated setters; carve-out setters (`on_process_task_tier2`/`on_process_task_streaming_tier2`/`on_transform_full`) write directly to wrapper fields. `into_table()` combines: `HandlerTable { generated: inner.into_table(), process_tasks, transform_full_handler }`. **Macro change**: generated `HandlerRegistry` visibility upgraded from `pub(crate)` to `pub` (the manual wrapper's `pub` Deref impl cannot leak a `pub(crate)` Target — Rust E0446); the inner `table` field is now `pub(crate)` (was private) so manual setter code accessing `self.table.<field>` resolves through Deref to the generated table. **Transparency check methods** (`is_input_transparent` / `is_lifecycle_transparent` / `is_fully_transparent`) moved from `HandlerTable` impl to `HandlerRegistry` impl in `input.rs` — the predicates need both the generated table (for the per-spec-entry transparency flags) and the carve-out `process_tasks` (for the per-task `transparent` flag), and the wrapper `HandlerRegistry` has both halves directly reachable via `self.inner.table` + `self.process_tasks`. Carve-out setter sites updated: `on_process_task_tier2`/`on_process_task_streaming_tier2` now `self.process_tasks.push(...)` (not `self.table.process_tasks`); `on_transform_full` now `self.transform_full_handler = Some(erased)` (not `self.table.transform_full_handler`). 4 files touched: `kasane-macros/src/handler_table.rs` (visibility upgrade), `handler_registry/mod.rs` (struct + Deref + new/into_table rewrite), `handler_registry/{lifecycle, transform, input}.rs` (carve-out site fixes + transparency check rewrite). All 1929 lib tests pass; workspace build + clippy + fmt clean. **The non-carve-out manual setter code in `decoration.rs`/`extension.rs`/`input.rs`/`lifecycle.rs`/`render.rs`/`transform.rs` now shadows the generated counterparts** (Rust prefers inherent impls over Deref'd methods); γ-3.3c-5b deletes the redundant shadows so plugin code uniformly invokes the generated setters. |
| γ-3.3c-4c — `HandlerTable` wrapper-struct refactor | ✅ 2026-05-13 | The manual `HandlerTable` is now a 3-field wrapper around the macro-generated table: `{ generated: gen::HandlerTable, process_tasks: Vec<ProcessTaskEntry>, transform_full_handler: Option<ErasedFullTransformHandler> }`. `Deref`/`DerefMut` to `gen::HandlerTable` make the ~100 field-access sites in `plugin_bridge.rs` and `handler_registry/*.rs` transparently route to the generated table without any rewrites; carve-out fields are accessed directly. **`TransparencyFlags`** retired as a manual struct — re-exported from generated, and the manual `is_all_*_transparent` predicates that included the `process_tasks` carve-out check were lifted to `HandlerTable::is_input_transparent` / `is_lifecycle_transparent` / `is_fully_transparent` wrapper methods (the public `HandlerRegistry::is_*_transparent` accessors in `input.rs` now delegate to these). Spec change: `key_middleware` gained `transparent` modifier (the manual setter already tracked transparency via `R: Transparency` bound; spec was the outlier). Field-name renames in 5 setter sites for `transparency.X` to align with macro convention (`key_handler→key`, `mouse_handler→handle_mouse`, `drop_handler→handle_drop`, `command_error_handler→command_error`, `subscription_handler→subscription`). One borrow-checker fix in `plugin_bridge::refresh_key_groups` — Deref method calls cannot be borrow-split, so an explicit `&mut self.table.generated` binding restores split borrow on `group_refresh_handler` + `key_map`. The `erased_aliases_type_equal_per_shape` parallel-impl test (250 LoC, 50 assert_type_eq! calls) retired — structural alignment is now proven by re-exports of entry structs / `DisplayRecoveryStatus` / `TransparencyFlags` rather than by alias-by-alias type-eq checks. The 50 manual erased aliases marked `#[allow(dead_code)]` (they remain referenced by trybuild trybuild infrastructure, and may be cleaned up further in γ-3.3c-5). All 1929 lib tests pass (-1 from retired test); workspace build + clippy + fmt clean. **Storage shape unification complete** — manual side now adds nothing structural beyond the 2 carve-out fields; γ-3.3c-5 next deletes the `handler_registry/*.rs` setter modules wholesale and replaces with `impl` blocks for the carve-out setters only (`define_projection`, `on_key_map`, `on_state_changed_for`, `on_transform_full`, `on_process_task_*`). |
| γ-3.3c-4b — entry-struct re-exports | ✅ 2026-05-13 | Manual entry-struct definitions retired in favour of `pub(crate) use crate::plugin::handler_table_spec::generated::*` re-exports — the spec module is now the canonical owner of every metadata-bearing entry type. Six re-exports landed: `ContributeEntry`, `TransformEntry`, `GutterEntry`, `ProjectionEntry` (in `handler_table.rs`); `PublishEntry`, `SubscribeEntry` (in `pubsub.rs`). The `ContributeAnyEntry` single-field wrapper (`{ handler: ErasedContributeAnyHandler }`) was deleted entirely — `contribute_any_handler` now stores `Option<ErasedContributeAnyHandler>` matching the generated layout; setter site dropped the wrapper construct, dispatch site dropped the `&entry.handler` accessor. `DisplayRecoveryStatus` enum re-exported from generated (the manual definition was shadowing causing setter-site type mismatches once `ProjectionEntry.recovery: gen::DisplayRecoveryStatus` became canonical); `is_visually_faithful` lifted from a method to a free function (orphan rule blocks adding methods to a re-exported foreign type). Three retired manual aliases: `ErasedContributeHandler`, `ErasedTransformHandler`, `ErasedGutterHandler` (the entry-struct re-exports own the type via `gen::Erased*Handler` inline); `ErasedPublisher` / `ErasedSubscriber` retired from `pubsub.rs` (no remaining consumers). Three corresponding `assert_type_eq_all!` checks in `handler_table_spec.rs` retired (manual aliases that no longer exist cannot be type-equated). 5 files touched: `handler_table.rs` (struct deletions + re-export block + 3 alias retirements + import cleanup), `handler_registry/transform.rs` (ContributeAnyEntry → raw handler at setter site), `plugin_bridge.rs` (dispatch site simplified), `pubsub.rs` (struct + alias deletions + re-export), `handler_table_spec.rs` (3 assertions retired); `handler_registry/decoration.rs` (`is_visually_faithful` → free function call). All 1930 lib tests pass; workspace build + clippy + fmt clean. **Storage-type unification complete** — every metadata-bearing entry type and the `DisplayRecoveryStatus` enum now have a single canonical definition (in the generated module). Only the field-level wiring of `HandlerTable` itself remains divergent (manual lists each field, generated does too — γ-3.3c-4c collapses manual into a wrapper holding `generated: gen::HandlerTable` + 2 carve-outs). |
| γ-3.3c-4a — `TransformEntry.full_handler` carve-out lift | ✅ 2026-05-13 | The manual `TransformEntry` struct previously embedded `full_handler: Option<ErasedFullTransformHandler>` alongside the patch handler — a structural divergence from the macro-generated `TransformEntry { priority, targets, handler }` (the `full_fallback` carve-out per spec §9.4 cannot live on the generated entry struct). γ-3.3c-4a lifts the carve-out companion to a top-level `HandlerTable.transform_full_handler: Option<ErasedFullTransformHandler>` field, leaving manual `TransformEntry` structurally identical to the generated one. Touches 3 files: `handler_table.rs` (struct field removed + new top-level field + `empty()` init updated + docstring rewrite), `handler_registry/transform.rs` (3 sites — `on_transform` and `on_transform_for` no longer carry forward `existing_full` since the field lives separately; `on_transform_full` writes directly to the new top-level field instead of get_or_insert-ing a TransformEntry shell), `plugin_bridge.rs` (1 dispatch site simplified — `self.table.transform_full_handler` instead of `entry.full_handler` from the patch entry). Behavioral note: `on_transform_full` previously created a placeholder `TransformEntry { priority: 0, targets: [], handler: identity }` if no patch handler existed; now it doesn't — the bridge's `transform()` consults the patch entry first (returns `Some(patch)` or `None`) then falls through to `transform_full_handler`. Net behavior: identical, since the previous placeholder patch was identity and the dispatch logic already short-circuits identity. All 1930 lib tests pass; workspace build + clippy + fmt clean. **Manual `TransformEntry` now structurally matches generated** — γ-3.3c-4b next consolidates `HandlerTable` itself into a wrapper around generated + 2 carve-out fields (`process_tasks`, `transform_full_handler`). |
| γ-3.3c-3 — per_slot+recovery storage alignment | ✅ 2026-05-13 | Macro extension: when a spec entry has both `per_slot = K` and `recovery` (currently only `projection`), recovery moves from the previously-orphaned table-level `<name>_recovery: DisplayRecoveryStatus` field to a per-entry `recovery: DisplayRecoveryStatus` field on the generated `<Name>Entry` struct. This matches what the manual side already does — each `define_projection` call carries its own recovery hint derived from the descriptor's `Structural` / `Additive` category. The generated `on_<name>(key, recovery, handler)` setter accepts the recovery argument; `define_projection` (carve-out spec §9.1) becomes a thin wrapper that derives recovery and delegates. Macro changes (3 functions in `kasane-macros/src/handler_table.rs`): `handler_table_field` skips table-level emission when `per_slot+recovery`; `handler_table_init` mirrors the skip; `entry_struct_decl` adds a `recovery` field when `per_slot+recovery`; `registry_setter_per_slot` adds a `recovery: DisplayRecoveryStatus` parameter and includes it in the entry constructor. Spec doc §6.3 + roadmap updated. New parallel-impl test `projection_recovery_lives_per_entry` constructs a `generated::ProjectionEntry` with `recovery: Witnessed(...)` and asserts the entry round-trips through the registry's `on_projection(descriptor, recovery, handler)` setter — proves both the storage shape and the new setter signature. 1930 lib tests pass (+1); workspace build + clippy + fmt clean. **Storage shape is now fully macro-derivable** — the only remaining structural divergence between manual and generated `HandlerTable` is the carve-out fields (`process_tasks: Vec<ProcessTaskEntry>`, `transform_handler.full_handler: Option<...>`); γ-3.3c-4 next consolidates these into a wrapper struct so γ-3.3c-5 can delete `handler_registry/*.rs`. |
| γ-3.3c-2 — render + input setter-name alignment | ✅ 2026-05-13 | Remaining 6 public setter API renames batched as a single sed pass (with `\b` word boundaries to avoid `on_drop` aliasing into `on_drop_tier1` etc.): `on_render_menu_overlay → on_menu_renderer`, `on_render_info_overlays → on_info_renderer`, `on_refresh_key_groups → on_group_refresh`, `on_invoke_action → on_action`, `on_drop_tier1 → on_handle_drop_tier1`, `on_drop → on_handle_drop`. 18 files touched: `handler_registry/{render, input, extension, tests}.rs` (setter decls + intra-module refs), `handler_table.rs` + `handler_table_spec.rs` (relevant TransparencyFlags / capability-inference accesses unaffected; doc-comment refs only), `plugin_bridge.rs` (1 dispatch site), `plugin/{registry/mod, effect/effects, tests/registry}.rs` (3 consumer/test sites), `state/update.rs` + `state/tests/update.rs` (1 each — call-through tests), `input/builtin_fold.rs` (1 builtin plugin call site), `render/view/{menu, info}.rs` (2 setter call sites for menu/info renderer factories), `kasane-wasm/src/{adapter, convert/display}.rs` (2 WASM bridge sites), `kasane-plugin-sdk-macros/src/defaults.rs` (1 SDK macro generated-code site). All 1929 lib tests pass; workspace build + clippy clean. **Manual setter API now uniformly `on_<spec_name>`** matching macro convention; γ-3.3c-3 next consolidates `HandlerTable` storage (wrap generated table + carve-out fields) so the manual setter bodies in handler_registry/*.rs become thin facades over the unified table — unblocks γ-3.3c-4 (deletion). |
| γ-3.3c-1 — decoration setter-name alignment | ✅ 2026-05-13 | Public setter API surface for the decoration axis aligned to macro-generated convention (`on_<spec_name>`). 6 renames batched as a single sed pass across 14 files: `on_decorate_gutter → on_gutter`, `on_decorate_background → on_background`, `on_decorate_inline → on_inline`, `on_display_unified → on_unified_display`, `on_display_unified_safe → on_unified_display_safe`, `on_render_ornaments → on_render_ornament`. 58 occurrences updated total: `handler_registry/decoration.rs` (8 setter decls + intra-doc refs), `handler_registry/{tests,mod}.rs` (call sites + helper refs), `plugin/{state.rs, plugin_bridge.rs}` (3+2 dispatch / capability refs), `plugin/tests/{hooks, registry}.rs` (3+6 test plugins), `widget/plugin.rs` (3 widget consumers), `state/shadow_cursor/mod.rs` (1), `render/view/tests.rs` (2), `tests/plugin_integration.rs` (1), `kasane-wasm/src/adapter.rs` (4 WASM bridge sites), `kasane-macros/src/{lib.rs, plugin.rs}` (2 docstring + 3 generated-code call sites). The `#[decorate_*]` attribute names on `#[plugin]` proc macro stayed (user-facing attribute API; describes what the function does, not where it's registered). All 1929 lib tests pass; workspace build + clippy clean. **γ-3.3c-2 next** addresses the remaining 6 renames in render.rs (`on_render_menu_overlay → on_menu_renderer`, `on_render_info_overlays → on_info_renderer`, `on_refresh_key_groups → on_group_refresh`, `on_invoke_action → on_action`) and input.rs (`on_drop → on_handle_drop`, `on_drop_tier1 → on_handle_drop_tier1`); after both lands, every public manual setter name aligns to `on_<spec_name>`, unblocking γ-3.3c-3's storage consolidation. |
| γ-3.3b-4 — `process_task` moved to carve-out | ✅ 2026-05-13 | The spec entry `handler process_task(_result: &ProcessTaskResult, _app: &AppView<'_>): Lifecycle<Effects>(tier2, transparent);` was generating an `Option<ErasedProcessTaskHandler>` storage that did not match the manual `Vec<ProcessTaskEntry { name, spec, handler, streaming, transparent }>` shape — Vec-of-metadata-Lifecycle keyed by `&'static str` with a registration-time `ProcessTaskSpec` payload + per-entry `streaming` flag does not generalize through the macro's per_slot path (single key + handler [+priority]). Extending the macro for one entry would compromise the path's clarity. **Resolution:** add `process_task` to spec §9 carve-out list (now 5 entries: `define_projection`, `on_key_map`, `on_state_changed_for`, `on_transform_full`, `on_process_task`); remove the spec entry; the storage and `on_process_task_tier2` / `on_process_task_streaming_tier2` setters in `handler_table.rs` + `handler_registry/lifecycle.rs` stay hand-authored alongside the macro-generated members. Touches 2 files: `handler_table_spec.rs` (entry + use deletion + test count 58→57 + carve-out comment), `docs/handler-table-dsl.md` (§9.5 added; §8.1, §8.5, §8.7 cross-references updated). All 1929 lib tests pass; workspace build + clippy clean. **γ-3.3b complete (b-1/b-2/b-3/b-4 ✅).** |
| γ-3.3b-3 — recovery split into per-name fields | ✅ 2026-05-13 | Manual `recovery: RecoveryFlags { display: DisplayRecoveryStatus }` (single shared slot that the `on_display_unified*` setters were silently clobbering — bug surfaced during the audit) split into per-handler-name fields: `display_recovery: DisplayRecoveryStatus`, `unified_display_recovery: DisplayRecoveryStatus`. Matches the macro's generated layout (`<name>_recovery` per `recovery`-marked entry). `RecoveryFlags` struct deleted; `is_visually_faithful()` lifted onto `DisplayRecoveryStatus` itself; `is_display_recoverable()` ANDs both fields. The `projection` carve-out's per-entry `ProjectionEntry.recovery` stays per-entry (each registration carries its own recovery hint — `Structural → Witnessed`, `Additive → NonDestructive`); the spec doc §8.6 + §6.3 updated to reflect the per-name representation. New parallel-impl test `recovery_fields_default_to_not_registered` confirms both manual and generated `HandlerTable::empty()` carry `display_recovery == NotRegistered` && `unified_display_recovery == NotRegistered`. Touches 3 files: `handler_table.rs` (struct retire + 2 fields), `handler_registry/decoration.rs` (5 setters + 1 query rewired), `handler_table_spec.rs` (1 new test + comment refresh). All 1929 lib tests pass; workspace build + clippy clean. **Process_task structural alignment deferred to γ-3.3b-4** — manual `process_tasks: Vec<ProcessTaskEntry { name, spec, handler, streaming, transparent }>` is a Vec-of-metadata-Lifecycle storage shape the macro doesn't yet support; spec entry currently emits the wrong `Option<ErasedProcessTaskHandler>` shape but is harmless dead generated code (no consumer). γ-3.3b-4 either extends the macro with a `Vec<>+metadata` Lifecycle storage variant, or adds `process_task` to the carve-out list (spec §9). |
| γ-3.3b-2 — per_slot Entry internal field + struct renames | ✅ 2026-05-13 | 5 per_slot Entry struct internal fields renamed to the macro's generic `key` slot: `ContributeEntry { slot → key }`, `GutterEntry { side → key }` (struct also renamed from `GutterHandlerEntry → GutterEntry` to match macro convention), `SubscribeEntry { topic → key }`, `PublishEntry { topic → key }`, `ProjectionEntry { descriptor → key }`. Touches across 7 files: `handler_table.rs` (struct decls + capability-inference accesses), `pubsub.rs` (PublishEntry/SubscribeEntry decls), `handler_registry/{mod,decoration,extension,transform,tests}.rs` (constructor sites + use-import update for the GutterEntry rename), `plugin_bridge.rs` (5 dispatch-loop accesses), `semantic_zoom/mod.rs` (test). All 1928 lib tests pass; workspace build + clippy clean. The semantic shift (e.g. `entry.descriptor.id` → `entry.key.id`) is mechanical — the type behind `key` is still the original `ProjectionDescriptor`/`SlotId`/etc, only the field name changes. |
| γ-3.3b-1 — top-level field-name alignment | ✅ 2026-05-13 | 8 manual `HandlerTable` field names renamed to match macro-generated counterparts: `subscribers → subscribe_handlers`, `publishers → publish_handlers`, `inline_box_paint_handler → paint_inline_box_handler`, `surfaces_factory → surfaces_handler`, `lenses_factory → lenses_handler`, `projection_entries → projection_handlers`, `key_map_builder → key_map_builder_handler`, `state_hash_handler → state_hash` (config-style, no `_handler` suffix). 19 consumer references updated across 5 files (`handler_table.rs` decl + init + capability inference; `handler_registry/{lifecycle,decoration,extension}.rs` setters; `semantic_zoom/mod.rs` test; `plugin_bridge.rs` dispatch sites). All 1928 lib tests pass; workspace build + clippy clean. Top-level field names now align between manual and generated; γ-3.3b-2 next addresses per_slot Entry struct internal field renames (`slot/side/topic/descriptor → key`) which is the larger remaining alignment work. |
| γ-3.3a-1 — alias-name audit + type-eq expansion | ✅ 2026-05-13 | `handler_table_spec.rs` 352 → 511 LoC (+159). Audited all 55 manual `Erased<Name>Handler` aliases against macro-generated counterparts; 47 names match exactly, 7 mismatches documented inline (`ErasedSurfacesFactory ↔ ErasedSurfacesHandler`, `ErasedLensFactory ↔ ErasedLensesHandler`, `ErasedKeyMapBuilder ↔ ErasedKeyMapBuilderHandler`, `ErasedAnnotateGutterHandler ↔ ErasedGutterHandler`, `ErasedAnnotateBackgroundHandler ↔ ErasedBackgroundHandler`, `ErasedAnnotateInlineHandler ↔ ErasedInlineHandler`, `ErasedInlineBoxPaintHandler ↔ ErasedPaintInlineBoxHandler`). The expanded `erased_aliases_type_equal_per_shape` test calls `assert_type_eq_all!` on all 47 matching aliases (16 Lifecycle + 7 Observer + 5 Dispatcher + 19 View) — each compile-time check confirms the macro's emitted signature is structurally identical to the manual one. Mismatches are pure naming (the underlying types match); γ-3.3a-2 absorbs the renames. |
| γ-3.3a-2 — rename mismatched manual aliases | ✅ 2026-05-13 | 7 manual aliases renamed in `handler_table.rs` (and 1 consumer in `handler_registry/lifecycle.rs:296`): `ErasedSurfacesFactory → ErasedSurfacesHandler`, `ErasedLensFactory → ErasedLensesHandler`, `ErasedKeyMapBuilder → ErasedKeyMapBuilderHandler`, `ErasedAnnotateGutterHandler → ErasedGutterHandler`, `ErasedAnnotateBackgroundHandler → ErasedBackgroundHandler`, `ErasedAnnotateInlineHandler → ErasedInlineHandler`, `ErasedInlineBoxPaintHandler → ErasedPaintInlineBoxHandler`. Each rename was a `replace_all` in handler_table.rs (declaration + field type reference); only `ErasedSurfacesFactory` had an external consumer. Type-equality assertion list expanded from 47 to 54 entries — every newly-aligned alias structurally matches the macro-generated counterpart on first compile. `ErasedFullTransformHandler` stays unaligned (full_fallback carve-out — γ-3.3 hand-writes the on_transform_full setter). Workspace build + clippy clean across all 7 crates. |
| γ-4 — Hot-path optimization | ✅ 2026-05-13 | All three sub-stages landed: γ-4.1 (`PluginId(String) → Arc<str>`), γ-4.2 (`Vec<Vec<Atom>>` scratch — paint_decorated allocs −47.8%, bytes −60.4%), γ-4.3 (closed by δ-2.1; `dyn-clone` workspace dep retired). |
| γ-4.1 — `PluginId(String)` → `PluginId(Arc<str>)` | ✅ 2026-05-13 | `kasane-core/src/plugin/mod.rs`: `PluginId(pub String)` → `PluginId(pub Arc<str>)`. Cloning is now an atomic refcount bump (one-shot ~3 ns vs. ~25 ns for the prior heap-alloc + memcpy). Manual `Serialize`/`Deserialize` impls (no `serde rc` feature dependency) preserve `#[serde(transparent)]` wire format — JSON `"plugin_id": "foo"` round-trips unchanged. Five `From` impls cover the call-site shapes (`&str`, `String`, `&String`, `Arc<str>`, `Box<str>`) so most `PluginId("foo".into())` sites compile unchanged via `&str → Arc<str>` inference; legacy `PluginId("foo".to_string())` patterns rewritten to `PluginId::from("foo")` (~394 occurrences across 113 files). New `as_str()` accessor + `Display` + `Borrow<str>` impls support tracing-field formatting and HashMap<PluginId, _>::get(&str) lookups. WIT boundary in `kasane-wasm/src/convert/command.rs` (3 sites for `ScheduleTimer` / `PluginMessage` / `SessionReadyCommand::PluginMessage`) and `kasane-wasm/src/host.rs` (3 sites for `selection_set_save` / `selection_set_load` / `register_command`) lifted to `PluginId::from(target_plugin.as_str())` — single Arc allocation at the host-guest boundary instead of `String → PluginId(String)` re-wrap. The `kasane_plugin` proc macro generates `PluginId::from(#id_str)` (string literal → Arc<str>, single alloc). Touches: 113 files, +1116/-3144 LoC (the dominant ~3000 deletions are γ-3 retirements; γ-4.1's net contribution is +500/-300 LoC concentrated in `plugin/mod.rs` (+60), the macro emit site (-1), and call-site rewrites). All 1928+ lib tests pass; workspace build + clippy + fmt clean across `default` / `--features gui` / `--features syntax` configurations. **Hot-path benefit**: every `Plugin::id()` clone (called once per dispatch per registered plugin per frame — 15+ sites in `manager.rs` / `plugin_bridge.rs` / `pubsub.rs` / `widget/plugin.rs` / `wasm/adapter.rs`) drops from ~25 ns/heap-alloc to ~3 ns/refcount; with ~10-20 active plugins this saves 0.4–4.4 µs per frame as projected. |
| γ-4.2 — `Vec<Vec<Atom>>` scratch pattern | ✅ 2026-05-13 | New `AtomScratch { decorated, decorated_for_gpu, inline_box_slots }` reusable buffer (`kasane-core/src/render/mod.rs`) threaded through the per-line paint loop. Inline-decoration helpers gained `_into` variants: `apply_inline_ops_into(atoms, decoration, &mut Vec<Atom>)` and `apply_inline_ops_for_gpu_into(..., atoms_out, slots_out)` write into caller-owned buffers (cleared first); the existing `apply_inline_ops` / `apply_inline_ops_for_gpu` are now thin allocate-and-delegate wrappers preserved for non-hot-path callers (tests, ad-hoc utilities). `BufferLineAction::BufferLine` field types changed: `decorated: Option<Vec<Atom>>` → `Option<&'a [Atom]>`, `decorated_for_gpu` likewise, `inline_box_slots: Vec<InlineBoxSlotMeta>` → `&'a [InlineBoxSlotMeta]`. `analyze_buffer_line` gained an `&'a mut AtomScratch` parameter; the action's slice fields borrow from the scratch until the action is dropped, then the scratch is freely reusable for the next iteration (Rust's NLL handles the lifetime correctly). `paint_buffer_ref` (TUI) and `walk_scene::visit_buffer_ref` (GPU) each allocate one `AtomScratch::new()` outside their per-line loop. The GPU path copies `inline_box_slots.to_vec()` into `BufferParagraph` (owned data outlives the paint pass — empty slots for non-InlineBox lines clone a zero-length Vec, no alloc). 17 test call sites updated to pass `&mut crate::render::AtomScratch::new()`. `alloc_budget` bin extended with a `paint_decorated` measurement (constructs `Element::BufferRef` directly with a `Style` op + EOL virtual text on every line — 23-line buffer, 80×24 frame). **Measured win**: paint_decorated allocs 46 → 24 (−47.8 %), bytes 6992 → 2768 (−60.4 %) — both well above the −30 % roadmap target. All 1928 lib tests pass; clippy + fmt clean across `default` / `--features gui` / `--features syntax` configurations. Touches: 4 production files (`render/mod.rs`, `render/inline_decoration.rs`, `render/paint.rs`, `render/walk_scene.rs`) + alloc_budget bin extension. **Not changed**: `walk_grid.rs` (re-uses `paint_buffer_ref`, inherits the optimization transparently); the `BufferParagraph.inline_box_slots` ownership boundary (queued draw commands outlive the paint pass — must remain owned). |
| γ-4.3 — `dyn_clone` snapshot reduction | ✅ 2026-05-13 (closed by δ-2.1) | Snapshot field `prev_state` and the `DynClone` supertrait retired in δ-2.1; `dyn-clone` dep removed from the workspace. |
| δ-1 — `Plugin` / `StatelessPlugin` trait split | ✅ 2026-05-13 | New `pub trait StatelessPlugin: Send + 'static` (in `kasane-core/src/plugin/state.rs`) with two methods (`id() → PluginId`, `register(&mut HandlerRegistry<()>)`) — same shape as `Plugin` minus the `State` associated type. Blanket `impl<P: StatelessPlugin> Plugin for P { type State = (); ... }` auto-derives the full Plugin trait, so callers can `impl StatelessPlugin for X { ... }` without the `type State = ();` boilerplate. Coherence: a single type may impl `StatelessPlugin` *or* `Plugin` directly, but not both — the blanket would conflict. WasmPlugin migrated from `impl Plugin for WasmPlugin { type State = (); ... }` to `impl StatelessPlugin for WasmPlugin { ... }` (kasane-wasm/src/adapter.rs); 6 in-tree builtins migrated for consistency: `BuiltinInfoPlugin` / `BuiltinMenuPlugin` / `BuiltinInputPlugin` / `BuiltinDragPlugin` / `BuiltinFoldPlugin` / `BuiltinMouseFallbackPlugin` / `BuiltinShadowCursorPlugin` / `ProjectionStatusPlugin`. Test-fixture plugins in `plugin/tests/`, `plugin_bridge.rs`, etc. left as `impl Plugin` to keep the explicit-trait surface exercised. `kasane-core/src/plugin/mod.rs` exports the new trait alongside `Plugin` / `PluginState`. `docs/plugin-api.md` §6.1 example updated to use StatelessPlugin. All 1928 lib tests pass; clippy + fmt clean across `default` / `--features gui` / `--features syntax` configurations; `alloc_budget` numbers unchanged (zero runtime overhead — the blanket lowers to a direct call). |
| δ-2 — `PluginState` ↔ Salsa input unification | ✅ 2026-05-14 (within delivered scope; δ-2.3b-ii / δ-2.3b-iii rescoped) | Delivered: δ-2.1 (snapshot retirement via inline pre-swap compare — `dyn-clone` workspace dep retired), δ-2.2 (per-plugin `PluginStateRevisionInput` Salsa input), δ-2.3a (`slot.last_state_hash` retirement; `sync_state_revisions` absorbed into `prepare_plugin_cache(&mut KasaneDatabase)`), δ-2.3b-i (`ContributionCache` rev-keyed; `PluginStateRevisionInput` gains its first production non-writer consumer via `collect_contributions_cached` reading the input directly), δ-2.4 (bench + perf gate; `delta-24` criterion baseline established). **Rescoped** (deep analysis showed misalignment with stated promise): δ-2.3b-ii (`bridge.generation` retire — needs `PluginState: Hash` design, future ADR), δ-2.3b-iii (full tracked-query conversion of `contribute_to` / `decorate_*` — coarse-grained invalidation granularity does not improve over the current `any_X_needs_recollect()` predicate path; would require `KasaneDb` runtime accessor + `AppState` attach + plugin-side dependency declaration; rescoped to Phase ζ pending future architectural ADR). |
| δ-2.3b-i — `ContributionCache` rev-keyed | ✅ 2026-05-14 | `kasane-core/src/plugin/registry/mod.rs`: `ContributionCache.contributions: HashMap<(PluginId, SlotId), ContributionEntry>` where `ContributionEntry { rev_at_collection: u64, sourced: Option<SourcedContribution> }`. `kasane-core/src/plugin/registry/collection/contributions.rs`: `collect_contributions_cached` and `collect_contributions_with_sources_cached` gain `db: &dyn KasaneDb` and `dirty: DirtyFlags` parameters; per-slot freshness is now `cached.rev_at_collection == current_rev && !dirty.intersects(view_deps)` instead of reading the precomputed `slot.needs_recollect: bool`. `slot.needs_recollect` field stays for the four other collectors' outer gates (`any_contributor/annotator/overlay/display_transform_needs_recollect`) — those gates are unchanged; full retirement of the field would require gating the four uncached collectors via revision compare too, which adds collector-cache infrastructure that exceeds δ-2 scope. **Threading**: `dirty` is now plumbed through `event_loop::sync_salsa_for_render(db, state, registry, handles, dirty)` → `salsa_sync::sync_unified_display(..., dirty)` → `sync_plugin_contributions(..., dirty)` → collector. Caller updates: `kasane-tui/src/lib.rs:578`, `kasane-gui/src/app/render.rs:97`. Test fixtures updated: `test_support.rs::render_to_grid_with_result`, `render/tests/{scene_cache, cursor_position}.rs`, `plugin/tests/registry.rs::test_contribution_cache_reuses_non_stale_plugin`. Bench fixtures: `kasane-core/benches/{rendering_pipeline, replay}.rs`, `kasane-tui/benches/backend.rs`, `kasane-gui/benches/cpu_rendering.rs`. Integration tests: `kasane-core-tests/tests/{latency_budget, rendering_pipeline}.rs`. **Outcome**: `PluginStateRevisionInput` is now read by production non-writer code (the contribution-collection cache); the input's "Salsa-shaped data with no consumer" status (recorded in δ-2.2's closure note) is resolved for at least one collector. **What δ-2.3b-i deliberately does NOT do** (deep analysis surfaced these as misaligned with stated δ-2 promise): (a) does not delete `slot.needs_recollect` (still drives 4 outer gates), (b) does not retire `bridge.generation` (state_hash() fallback path unchanged — this is δ-2.3b-ii, requires Hash trait design), (c) does not convert `contribute_to`/`decorate_*` to `#[salsa::tracked]` queries (rescoped to Phase ζ — coarse-grained invalidation granularity gain is structurally zero per the dependency-tracking analysis). **Verification**: 1893 kasane-core lib tests pass (+1 after `test_contribution_cache_reuses_non_stale_plugin` rewrite); workspace `cargo test` clean except pre-existing `kasane-macros` trybuild link failures (rust-lld + salsa rlib `sh_addralign` issue, reproduces on master without these changes); clippy `--workspace --lib -- -D warnings` clean across `default` / `--features gui` / `--features syntax`; `cargo build --no-default-features` (TUI-only) passes; fmt + `tools/check-doc-consistency.sh` clean. |
| δ-2.4 — Bench + perf gate | ✅ 2026-05-14 | `cargo bench --bench rendering_pipeline -- --baseline gamma` revealed apparent +75 to +175% regressions on `cached_pipeline_dirty_flags/*`, `scene_cache_*`, `line_dirty_*`, and `section_paint_*` — but root cause is **baseline staleness**, not δ-2: γ-1.1 (`6da05fa2`) deleted the legacy `render_pipeline_direct` / `scene_render_pipeline` non-Salsa entry points and the bench helpers were rewritten to `render_pipeline_via_salsa` / `scene_render_via_salsa`, each of which now does a per-iteration `KasaneDatabase::default()` + `SalsaInputHandles::new()` + `sync_inputs_from_state` + `sync_display_directives` + `sync_plugin_contributions` setup that the legacy path didn't pay. The gamma baseline (captured 2026-05-12 at program-open commit `220d0342`, before γ-1.1 landed) measures different code than the current source tree, so the comparison is apples-to-oranges for any bench routed through these helpers. **Apples-to-apples slice** (benches not routed through the post-γ-1.1 helpers — `paint`, `full_frame`, `draw_message`, `parse_request`, `state_apply`, `salsa_sync_inputs`, `detect_cursors`, `scaling/*`): 38 of 40 within ±5%, 2 outliers at +7-15% (`grid_diff/full_redraw` +12.5%, `salsa_sync_inputs/menu/100_items` +15.2%) on µs-scale benches where absolute drift is sub-µs. None of the regressed paths are touched by δ-2.x — `sync_inputs_from_state` is unchanged since gamma; `prepare_plugin_cache` (the only function δ-2.3a rewrote) does not appear in any bench iter loop. **Gate verdict**: G4 (±5% frame-time) passes for the δ-2 scope (no in-scope code changed cost beyond noise); the spurious "regressions" are tagged out as bench-helper-version artifacts. **Re-baselined**: criterion `new/` directories promoted to `delta-24` baseline (94 entries, vs gamma's 72 — the new baseline includes the post-γ-1.1 `salsa_full` / `salsa_scene` / `salsa_scaling` benches that gamma cannot cover). Future commits should compare against `delta-24`; gamma is retired but kept on disk for historical reference. **iai_pipeline**: skipped — no prior iai callgrind output exists locally to compare against (the criterion gamma baseline is on-disk; iai's would have needed to be captured separately at program-open). The first iai run from a future phase will establish its own baseline. |
| δ-2.3a — `slot.last_state_hash` retirement + sync absorption | ✅ 2026-05-14 | `PluginSlot::last_state_hash: u64` field deleted; the δ-2.2 `PluginStateRevisionInput` doubles as the canonical "last collected hash" via `input.revision(db)`. `PluginRuntime::sync_state_revisions(&mut self, db)` was retired; its work is absorbed into `prepare_plugin_cache(dirty, &mut KasaneDatabase)` which now performs the pre/post-swap compare against the Salsa input in one pass — reads `input.revision(db)` as the previous hash, calls `set_revision().to(current)` on change, and uses the comparison result to drive `slot.needs_recollect`. `event_loop::sync_salsa_for_render` no longer carries the separate revision-mirror call (the work moved upstream into the existing `prepare_plugin_cache` site that runs immediately before `sync_salsa_for_render`). Touches: 6 production files (`plugin/registry/mod.rs` field removal + `prepare_plugin_cache` rewrite, `event_loop/mod.rs` -5 lines, `kasane-tui/src/lib.rs` +`&mut salsa_db` arg, `kasane-gui/src/app/render.rs` +`&mut self.salsa_db` arg) + ~35 test sites across `plugin/plugin_bridge.rs`, `plugin/tests/registry.rs`, `kasane-core-tests/tests/plugin_integration.rs`, and `kasane-core-tests/tests/semantic_zoom_e2e.rs`. The semantic-zoom test helpers (`register_zoom_plugin`, `dispatch_key`) gained a `KasaneDatabase` companion so the per-slot Salsa-input handles remain valid across multiple `dispatch_key` invocations in the same test. Bridge-internal `generation: u64` and the `state_revision_at(idx, &db)` test accessor are kept; full retirement of those needs δ-2.3b's tracked-query conversion (a bridge mutation can't bump a Salsa input without `&mut db`). The `HASH_SENTINEL` constant remains for `last_group_refresh_hash`. All 1880 kasane-core lib tests + 176 kasane lib tests + workspace integration tests pass; clippy `--workspace --lib -- -D warnings` clean across `default` / `--features gui` / `--features syntax`; `cargo build --no-default-features` (TUI-only) passes; fmt + `tools/check-doc-consistency.sh` clean. |
| δ-2.2 — Per-plugin `PluginStateRevisionInput` Salsa input (foundation) | ✅ 2026-05-13 | New `#[salsa::input] struct PluginStateRevisionInput { revision: u64 }` in `kasane-core/src/salsa_inputs.rs`. `PluginSlot` gains `state_revision: Option<PluginStateRevisionInput>` — lazily initialized on first sync (the input constructor needs `&mut KasaneDatabase`, which the synchronous registration path doesn't have). New `PluginRuntime::sync_state_revisions(&mut self, db: &mut KasaneDatabase)` method: iterates slots, creates the input on first call, then `set_revision().to(current)` against the bridge's `state_hash()` on subsequent calls. Wired into `event_loop::sync_salsa_for_render` (just after `cleanup_unloaded_plugins`), so both TUI and GUI back-ends pick it up automatically. Scope is **minimal-additive**: the input has no consumers yet — `slot.last_state_hash` / `slot.needs_recollect` / `bridge.generation` all stay, the imperative `prepare_plugin_cache(dirty)` gating path is untouched. The input exists so δ-2.3's `#[salsa::tracked]` plugin-contribution queries (still to be written) have a per-plugin invalidation dependency to depend on. New test `sync_state_revisions_mirrors_bridge_state_hash` (`plugin/plugin_bridge.rs`) and test-only `PluginRuntime::state_revision_at(idx, db) -> Option<u64>` accessor validate that the input tracks the bridge's hash across mutation cycles + that Salsa's `set_*.to()` short-circuits no-op syncs. **Deferred to δ-2.3** (separate session): retire `bridge.generation` + `slot.last_state_hash` + `slot.needs_recollect`; convert `contribute_to` / `decorate_*` to Salsa-tracked queries. The blocker is that those queries need to invoke per-plugin handler-table closures owned by `PluginRuntime`, but `&dyn KasaneDb` has no accessor to reach the runtime — extending `KasaneDb` with a plugin-runtime accessor is the architectural decision δ-2.3 must answer (options: `Arc<Mutex<PluginRuntime>>`, dedicated render-phase borrow into `KasaneDatabase`, or interior-mutability refactor of `PluginRuntime`). Touches: 4 files (`salsa_inputs.rs` +1 input, `plugin/registry/mod.rs` +1 field on `PluginSlot` / +1 method on `PluginRuntime` / +1 test-only accessor, `plugin/plugin_bridge.rs` +1 test, `event_loop/mod.rs` +1 call in `sync_salsa_for_render`). All 1880 kasane-core lib tests (incl. the new one) + workspace tests pass; `cargo clippy --workspace --lib` clean across `default` / `--features gui` / `--features syntax`; `cargo fmt --check` + `tools/check-doc-consistency.sh` clean. |
| δ-2.1 — `dyn_clone` snapshot retirement (inline pre-swap compare) | ✅ 2026-05-13 | `PluginBridge` no longer carries a `prev_state: Box<dyn PluginState>` snapshot field. Change detection moves from post-swap `check_state_change()` (which compared `state` against `prev_state` and cloned `state` into `prev_state` on every real mutation) to a new `replace_state(&mut self, new_state)` helper that compares the handler's returned `new_state` against the still-owned `self.state` via `(*new_state).dyn_eq(&*self.state)` *before* the swap — generation bumps only when the two differ. The four dispatch macros (`dispatch_state_effect!`, `dispatch_state_only!`, `dispatch_optional_consume!`, `dispatch_state_with_default!`) and the two manual sites (`try_process_task_event`, `deliver_subscriptions`) route through `replace_state`; `deliver_subscriptions` inlines the compare body (rather than calling `self.replace_state`) because the surrounding `&self.table.subscribe_handlers` borrow conflicts with `&mut self` — per-field borrows on `self.state` / `self.generation` are disjoint from the table borrow. The `PluginState` trait drops its `DynClone` supertrait; `dyn_clone::clone_trait_object!(PluginState)` retired; `dyn-clone` workspace dep removed (`Cargo.toml`, `kasane-core/Cargo.toml`). Blanket `impl<T> PluginState for T` relaxed from `T: Clone + PartialEq + Debug + Send + 'static` to `T: PartialEq + Debug + Send + 'static` — concrete `Plugin::State` still requires `Clone` (line 78 bound) because handler bodies routinely call `state.clone()` on `&S`. Touches: 6 files (`kasane-core/src/plugin/state.rs` -1 supertrait / -1 macro / -1 `plugin_state_clone` test, `kasane-core/src/plugin/plugin_bridge.rs` -1 field / -1 method / +1 method / 8 dispatch sites, `kasane-core/src/plugin/handler_table.rs` -1 `dyn_clone::clone_box` test call, `kasane-core/Cargo.toml` -1 dep, `Cargo.toml` -1 workspace dep, `kasane-macros/tests/pass/handler_table_basic.rs` doc-comment refresh) + `docs/semantics.md` Tier 1 phrasing updated to "before the swap" wording. **Bench / perf impact**: zero per-mutation clone — handler that mutates state now pays one extra `dyn_eq` call (vtable dispatch + concrete compare, ~ns) and saves one `dyn_clone::clone_box` invocation (allocation + memcpy of state payload). Net win scales with state size. **γ-4.3 closed** by this sub-stage. All 1879 lib tests pass (1855 kasane-core + the rest); workspace build + `cargo clippy --workspace --lib` clean across `default` / `--features gui` / `--features syntax`; fmt + `tools/check-doc-consistency.sh` clean. |
| δ-3 — Example fleet curation | ✅ 2026-05-13 | In-tree example set shrunk to `cursor-line` + `color-preview` per the roadmap intent. **Deleted (13 directories)**: `examples/wasm/{sel-badge, fuzzy-finder, pane-manager, prompt-highlight, smooth-scroll, session-ui, image-preview, kakoune-bindings-demo, selection-algebra}` (9 WASM gallery-bound) + `examples/{image-test, line-numbers, selection-algebra-native, virtual-text-demo}` (4 native). Bundled set in `kasane-wasm/bundled/` reduced from 6 → 2: deleted `{sel-badge, fuzzy-finder, pane-manager, smooth-scroll}.{wasm,toml}`; `pane_manager` was the only `default_enabled = true` plugin so default-on behaviour is now empty (no plugin loads automatically; users must opt-in via `plugins.enabled`). `kasane-wasm/src/lib.rs` `bundled_plugin_specs()` now lists only `cursor_line` + `color_preview` (both `default_enabled = false`). Test infrastructure: deleted `kasane-wasm/src/tests/{fuzzy_finder, prompt_highlight, session_ui, smooth_scroll}.rs`; pruned `tests/mod.rs` of unused fixture loaders + stale imports; updated `tests/discovery.rs` (renamed `bundled_plugin_artifacts_include_default_enabled_pane_manager` → `bundled_plugin_artifacts_lists_curated_set` asserting the curated 2-plugin list; swapped `pane_manager` / `prompt-highlight` test-target IDs to `color_preview` / `color-preview`). Deleted 8 `kasane-wasm/fixtures/*.{wasm,toml}` for removed plugins. Updated `kasane/src/locked_wasm_provider.rs` test fixtures + `kasane/src/lib.rs::plugin_manager_reload_applies_added_plugins_only` to use kept fixtures (`cursor-line` / `color-preview` instead of `sel-badge` / `smooth-scroll`); retired `kasane/src/plugin_cmd/resolve.rs::reconcile_adds_default_enabled_bundled_plugin` test (no default-enabled plugin remains to exercise). `Cargo.toml` workspace `exclude` list reduced to the 2 kept examples + non-example exclusions. **Doc updates**: `README.md` example table reduced to 2 entries; the embedded code block now mirrors `cursor-line/src/lib.rs` (was sel-badge); demo-gif alt text updated. `tools/check-doc-consistency.sh::check_readme_code_examples` now compares against `cursor-line` source (was sel-badge). `CLAUDE.md` workspace structure table collapses 4 example rows into one. `docs/using-plugins.md` "Bundled WASM Plugins" table reduced to 2; "Additional Source Examples" section deleted. `docs/plugin-development.md` "Full Example: sel-badge" rewritten as "Full Example: cursor-line" (display-directive pattern); "Plugin Profiles" table marks moved entries with "(gallery)" suffix; cookbook-style references annotated with the moved-to-gallery note. `docs/plugin-cookbook.md` "See: examples/wasm/X" lines for moved examples annotated with the gallery note. `docs/semantics.md`, `docs/plugin-api.md`: incidental example-name references rephrased generically. **Future external repo**: `kasane-plugin-gallery` does not yet exist; the deleted plugin sources are recoverable from this repo's git log before this δ-3 cleanup commit. The original roadmap-stipulated link retargeting to the external gallery is deferred until that repo is created — current docs use phrasing like "moved to a future external `kasane-plugin-gallery`" instead of dead links. All 1928 lib tests + 176 kasane lib tests + 156 kasane-wasm lib tests pass; `bash tools/check-doc-consistency.sh` clean (2 warnings — pre-existing config.rs vs. config.md drift outside δ-3 scope); clippy + fmt clean across `default` / `--features gui` / `--features syntax` configurations. |
| δ-4 — Error handling unification | ✅ 2026-05-14 | 5-7 days; per-module `thiserror::Error` enum, `anyhow::Result` at binary boundary; `kasane-core/src/error/mod.rs` aggregate type. Sub-stages: δ-4.1 (aggregate `CoreError` + `Result<T>` alias foundation), δ-4.2 (manual `Display + Error` impls → `thiserror::Error` derive — 10 types), δ-4.3 (`Result<T, String>` retirement at typed-error boundaries — 8 functions across 3 crates), δ-4.4 (library-internal `anyhow` purge — 6 sub-stages: δ-4.4a `kasane-plugin-package`, δ-4.4b `kasane-core::config`, δ-4.4c `kasane-core` trait surfaces via `DynError`, δ-4.4d `kasane-tui`, δ-4.4e `kasane-gui`, δ-4.4f `kasane-wasm` + boundary-policy refinement), δ-4.5 (binary boundary — `kasane::run() -> anyhow::Result<()>` + `main -> ExitCode`; in-passing `CliError` conversion to `thiserror`). |
| δ-4.1 — `CoreError` aggregate + `Result<T>` alias foundation | ✅ 2026-05-14 | New `kasane-core/src/error/mod.rs` declares aggregate `pub enum CoreError` (derives `thiserror::Error`, marked `#[non_exhaustive]` so downstream sub-stages can extend variants without a breaking-change bump) and `pub type Result<T> = core::result::Result<T, CoreError>` alias. Initial variant set is intentionally minimal — only the two types that already implement `std::error::Error` upstream are wired in via `#[error(transparent)] #[from]`: `Config(UnifiedParseError)` from `crate::config::unified` and `Theme(ThemeError)` from `crate::render::theme`. The remaining 10 scattered library errors (`SessionManagerError`, `HistoryError`, `SaveError`, `LoadError`, `OwnerValidationError`, `SurfaceRegistrationError`, `WidgetParseError`, `WidgetNodeError`, `TemplateParseError`, `CondParseError`, `ConfigError`) still lack `Error` impls and will fold in via δ-4.2 once they gain `thiserror::Error` derives. `Result` is intentionally **not** re-exported at the crate root (would shadow `std::result::Result` for `use kasane_core::*` consumers); callers reach it via `kasane_core::error::Result`. Three unit tests in the new module validate `From` conversions and the `?` propagation pattern. Touches: 2 files (`kasane-core/src/error/mod.rs` new, `kasane-core/src/lib.rs` +1 module declaration). All 1859 `kasane-core` lib tests (incl. 3 new) pass; workspace `cargo clippy --lib -- -D warnings` clean across `default` / `--features gui` / `--features syntax`; `cargo fmt --check` + `tools/check-doc-consistency.sh` clean. Pure foundation: zero consumer migration in this sub-stage. |
| δ-4.5 — Binary boundary: `kasane::run() -> Result<()>` + `main -> ExitCode` | ✅ 2026-05-14 | `pub fn run(provider) -> anyhow::Result<()>` (was `-> ()` with inline `eprintln!("error: …"); std::process::exit(1)` in each branch). The 6 `match action { ... }` arms (`ShowVersion`, `ShowHelp`, `Init`, `DelegateToKak`, `Plugin`, `Widget`, `RunKasane`) now use `?` for the four fallible ones (`Init`, `Plugin`, `Widget`, `RunKasane`); errors auto-coerce into `anyhow::Error` via thiserror's blanket `From<E: StdError + Send + Sync + 'static>` impl. `cli::parse_cli_args(&args)?` replaces the manual `match { ... eprintln; exit }` block. `pub fn run_with_factories` / `pub fn run_without_plugins` propagate the same `anyhow::Result<()>` signature. **`main` rewrite**: `fn main() -> ExitCode { match kasane::run_without_plugins() { Ok(()) => ExitCode::SUCCESS, Err(e) => { eprintln!("error: {e}"); ExitCode::FAILURE } } }` — the single error-print site lives in `main` instead of being duplicated 6× across `run`'s match arms. **In-passing cleanup**: `CliError` migrated from manual `Display` impl (16 variants, 80 lines of `write!`) to `#[derive(thiserror::Error)]` with `#[error("…")]` per variant — wording preserved verbatim; the previously-missing `std::error::Error` impl now exists so `?` propagation into `anyhow::Result` works. Touches: 3 production files (`kasane/src/lib.rs`, `kasane/src/main.rs`, `kasane/src/cli.rs`). All 1892 kasane-core + 176 kasane + 248 kasane-gui + 45 kasane-tui + 156 kasane-wasm lib tests pass; clippy `--workspace --lib -- -D warnings` clean across `default` / `--features gui` / `--features syntax`; `cargo build --no-default-features` green; fmt + doc-consistency clean. **δ-4 closed**: per-module `thiserror::Error` enums in place, library-internal `anyhow` purged across all 5 crates (`kasane-core` / `kasane-tui` / `kasane-gui` / `kasane-wasm` / `kasane-plugin-package`), `anyhow::Result` confined to the binary boundary (`kasane::run`) and crate-private wasmtime-bridge helpers per the boundary-policy doc at `kasane-core/src/error/mod.rs`. |
| δ-4.4f — `anyhow` purge: `kasane-wasm` + boundary policy refinement | ✅ 2026-05-14 | `WasmPluginError::Other` switched from `#[source] anyhow::Error` to `#[from] anyhow::Error` so the typed error auto-coerces from anyhow inside `?` chains. A new `impl From<wasmtime::Error> for WasmPluginError` bridge maps `wasmtime::Error` (distinct from `anyhow::Error` in wasmtime 43) into `WasmPluginError::Other(err.into())`. New `WasmPluginError::Manifest(#[from] ManifestError)` variant so the 3 manifest-parse call sites (`bundled_plugin_manifest_by_plugin_id`, `load_bundled_plugin_by_plugin_id`, `bundled_plugin_artifact_from_spec`) replace the `WasmPluginError::Other(anyhow::anyhow!(err))` boilerplate with a typed `.map_err(WasmPluginError::from)`. `load_wasm_fixture` (the `#[doc(hidden)]` test helper) switches from `anyhow::Result<Vec<u8>>` to `std::io::Result<Vec<u8>>`. **Boundary policy refined**: the `kasane-core/src/error/mod.rs` module-level doc gains a third "anyhow is allowed" rule: crate-private helpers that bridge external opaque errors and feed `#[source]` slots may keep `anyhow::Result<T>` because converting to a typed `Result<T, FooError>` would require explicit `From<wasmtime::Error>` conversions at every call site that *erase the caller-side stage tagging* (`ComponentLoad` vs `Instantiate` vs `WasiContext`). The privacy boundary contains the leak: no `pub` API in any kasane crate now returns `anyhow::Result`. **Remaining legitimate `anyhow` in kasane-wasm**: the 5 `#[source]` slots in `WasmPluginError` (`EngineInit / ComponentLoad / Instantiate / WasiContext / Other`), 4 crate-private wasmtime-bridge helpers in `lib.rs` (`create_engine_and_linker`, `load_component`, `instantiate_component`, `instantiate_with_manifest`), 4 in `capability.rs` (the WasiCtx builders), and the adapter.rs dispatcher closure types (`impl FnOnce(&mut WasmPluginRuntime) -> anyhow::Result<R>`) which accept wasmtime runtime calls and route diagnostics through `&anyhow::Error` references. Touches: 2 production files. All 1892 kasane-core + 176 kasane + 248 kasane-gui + 45 kasane-tui + 156 kasane-wasm lib tests pass; clippy `--workspace --lib -- -D warnings` clean across `default` / `--features gui` / `--features syntax`; `cargo build --no-default-features` green; fmt + doc-consistency clean. δ-4.4 (`anyhow` library-internal purge) is now closed across `kasane-plugin-package` / `kasane-core` / `kasane-tui` / `kasane-gui` / `kasane-wasm`. |
| δ-4.4e — `anyhow` purge: `kasane-gui` | ✅ 2026-05-14 | Two new typed-error enums in `kasane-gui`: (a) `pub enum GpuError { CreateSurface, RequestAdapter, RequestDevice, NoCompatibleFormat, SurfaceAcquire, SurfaceValidation }` in `src/gpu/mod.rs` (`#[from]` wgpu's `CreateSurfaceError` / `RequestAdapterError` / `RequestDeviceError`, plus two custom variants for the post-reconfigure surface-acquire failure modes); (b) `pub enum GuiError { MissingPrimarySession, AcquirePrimarySession, EventLoop, Io, PluginManager }` in new `src/error.rs` (`#[from] SessionManagerError`, `#[from] winit::error::EventLoopError`, `#[from] std::io::Error`, plus `PluginManager(DynError)`). **`BackendError` refactor**: dropped the hand-written `Display + Error` impls + `From<anyhow::Error>` impl; now `#[derive(thiserror::Error)]` with `Unsupported(&'static str)`, `Render(#[from] DynError)`, `Gpu(#[from] GpuError)`. **`GpuState::new` / `acquire`** switch from `anyhow::Result` / `anyhow::bail!` to `Result<_, GpuError>` with explicit variant construction; the 4 `SceneRenderer::{render, render_to_target, render_inner, render_with_cursor}` (the trait-impl wrapper) signatures switch from `anyhow::Result<()>` to `Result<(), BackendError>`. **`run_gui`** now returns `Result<(), GuiError>`; the prior `anyhow!("missing primary session id")` / `anyhow!("failed to acquire primary session: …")` calls become `GuiError::MissingPrimarySession` / typed `?` propagation; `EventLoop::with_user_event().build()?` and `event_loop.run_app(&mut app_handler)?` flow through `#[from] winit::error::EventLoopError`. **`app::App::new`** changes return type to `Result<(Self, Vec<PathBuf>), GuiError>`; the `PluginManager::initialize` `DynError` wraps through `GuiError::PluginManager`. `kasane-gui/Cargo.toml` drops `anyhow`, gains `thiserror = { workspace = true }`. Binary boundary in `kasane/src/lib.rs::run_inner` adds one `.map_err(anyhow::Error::new)` after `run_gui` (mirrors the `run_tui` adapter from δ-4.4d). One clippy `useless_conversion` site cleaned in `scene_renderer/mod.rs::SceneRenderer::render_with_cursor` (the wrapper-trait impl) — the inner `render_with_cursor` already returns `Result<(), BackendError>` so the prior `.map_err(BackendError::from)` was a no-op. Touches: 8 production files. All 1892 kasane-core + 176 kasane + 248 kasane-gui + 45 kasane-tui + 156 kasane-wasm lib tests pass; clippy `--workspace --lib -- -D warnings` clean across `default` / `--features gui` / `--features syntax`; `cargo build --no-default-features` green; fmt + doc-consistency clean. |
| δ-4.4d — `anyhow` purge: `kasane-tui` | ✅ 2026-05-14 | New `pub enum TuiError { MissingPrimarySession, AcquirePrimarySession, Io, PluginManager }` in `kasane-tui/src/error.rs` (`#[from] SessionManagerError`, `#[from] std::io::Error`, plus `PluginManager(DynError)` for the user-extensible boundary alias from `kasane-core`). `kasane-tui/src/lib.rs::run_tui` now returns `Result<(), TuiError>`; the prior `anyhow!("missing primary session id")` / `anyhow!("failed to acquire primary session: …")` calls become `TuiError::MissingPrimarySession` / `?` on the now-typed `SessionManagerError`. `event_handler.rs::handle_plugin_reload` returns `Result<_, TuiError>` and wraps `PluginManager::reload`'s `DynError` via `TuiError::PluginManager`. `sgr.rs` (3 fns) and `backend.rs` (`TuiBackend::new` + `present`) switch from `anyhow::Result<()>` to `std::io::Result<()>` — these only wrap crossterm's `queue!` / `write_all` / `flush` which already return `io::Result`. `kasane-tui/Cargo.toml` drops `anyhow`, gains `thiserror = { workspace = true }`. Binary boundary in `kasane/src/lib.rs::run_inner` adds one `.map_err(anyhow::Error::new)` after `run_tui` so the `TuiError`-typed `Result` joins the `anyhow::Result` branch of the `match resolved_ui { ... }` (`run_gui` still returns `anyhow::Result` until δ-4.4e). Touches: 6 production files. All 1892 kasane-core + 176 kasane + 248 kasane-gui + 45 kasane-tui + 156 kasane-wasm lib tests pass; clippy `--workspace --lib -- -D warnings` clean across `default` / `--features gui` / `--features syntax`; `cargo build --no-default-features` green; fmt + doc-consistency clean. |
| δ-4.4c — `anyhow` purge: `kasane-core` trait surfaces via `DynError` | ✅ 2026-05-14 | New `pub type DynError = Box<dyn core::error::Error + Send + Sync + 'static>` alias in `kasane-core/src/error/mod.rs` (with the boundary-policy doc-comment explaining when implementors should use this trait-surface alias vs. the concrete `CoreError`). All `kasane-core` trait-method error parameters and the session-spawner function-pointer type migrated to `DynError`: `PluginFactory::create() -> Result<Box<PluginBridge>, DynError>`, `PluginProvider::collect() / update_config() -> Result<_, DynError>`, `PluginManager::{initialize, reload, plan_*, apply_plan, collect_and_resolve}() -> Result<_, DynError>`, `ReloadOrchestrator::resolve_and_signal_reload() -> Result<_, DynError>`, plus a new `pub type SpawnSessionFn<R, W, C> = fn(&SessionSpec) -> Result<(R, W, C), DynError>` re-exported through `event_loop` (the in-line fn-pointer was flagged by clippy's `type_complexity`). `anyhow` is fully retired from `kasane-core`: `Cargo.toml` drops the dep; `src/` has zero `anyhow::*` references (the lingering two doc-comment mentions are descriptive boundary-policy prose, not code). **Downstream callers** updated: `kasane/src/locked_wasm_provider.rs` (the lone production `PluginProvider` impl) converts its rwlock-poisoned errors from `anyhow::anyhow!` to `DynError::from(&str)`; `kasane/src/orchestrator.rs::DefaultReloadOrchestrator` converts its `anyhow::Result` source from `resolve_and_save` to `DynError` via `.map_err(Into::into)` (which works because `anyhow::Error: Into<Box<dyn Error + Send + Sync + 'static>>`); test-fixture impls inside `kasane-core/src/plugin/manager.rs` and `kasane/src/lib.rs::run_inner::tests` updated likewise. `kasane-tui` / `kasane-gui` keep their internal `anyhow::Result` boundary on `run_tui` / `run_gui` for now (binary-boundary anyhow remains legitimate at the `kasane::run` callsite, per the existing policy) but their `?`-propagation from `PluginManager::initialize` / `reload` and the `spawn_session` fn-pointer type aliasing went through one mechanical conversion: `?` on `Result<_, DynError>` in an `anyhow::Result` context goes through `.map_err(anyhow::Error::from_boxed)?` (preserves source chain), and the fn-pointer type alias is now `kasane_core::event_loop::SpawnSessionFn<R, W, C>`. **Binary adapter**: `kasane/src/lib.rs::run_inner` gains a 12-line `fn spawn_session_adapter` shim that wraps `process::spawn_kakoune_for_spec` (`anyhow::Result`) into the new fn-pointer-required `Result<_, DynError>` via `.map_err(Into::into)`. **Not converted in this sub-stage**: `kasane-tui` / `kasane-gui` / `kasane-wasm` still depend on `anyhow` internally (downstream sub-stages); the kasane binary continues to use `anyhow::Result` at top level per binary-boundary policy. Touches: ~10 production files. All 1892 kasane-core lib tests + 176 kasane lib tests + 248 kasane-gui lib tests + 45 kasane-tui lib tests + 156 kasane-wasm lib tests + workspace integration tests pass; clippy `--workspace --lib -- -D warnings` clean across `default` / `--features gui` / `--features syntax`; `cargo build --no-default-features` green; fmt + doc-consistency clean. |
| δ-4.4b — `anyhow` purge: `kasane-core::config` load/save IO | ✅ 2026-05-14 | New `pub enum ConfigLoadError { ReadFile, CreateDir, WriteFile, AtomicRename, KdlSerialize }` in `kasane-core/src/config/mod.rs` (each variant pins the offending `PathBuf` next to `#[source] std::io::Error`, except `KdlSerialize` which wraps `#[source] kdl::KdlError`); `Config::try_load`, `Config::try_load_from_path`, `Config::save`, `Config::save_to_path` switch from `anyhow::Result` + `with_context` / `anyhow!` chains to `Result<_, ConfigLoadError>` (or `Result<_, CoreError>` for the load path that also bubbles `UnifiedParseError`). `CoreError` gains a `ConfigLoad(#[from] ConfigLoadError)` variant so `?` propagation works end-to-end. The unified parse error (previously routed through `anyhow!("{e}")`) now flows via `?` thanks to its existing `#[from]` in `CoreError`. Binary-boundary callers in `kasane/src/plugin_cmd/*` and `kasane/src/orchestrator.rs` continue to use `?` (the typed `CoreError` / `ConfigLoadError` auto-coerce into `anyhow::Error` through thiserror's blanket `Error` impl). **Not yet retired** in this sub-stage: `anyhow` remains in `kasane-core` for the `PluginFactory::create` / `PluginProvider::collect` / `PluginProvider::update_config` / `PluginManager::reload` / `ReloadOrchestrator::resolve_and_signal_reload` trait surfaces (user-extensible — implementors live in `kasane-wasm` and the `kasane` binary, with diverse internal error sources) and the `event_loop::session.rs::spawn_session` function-pointer type (caller-defined error type via the `kasane` binary's session-spawner). The remaining `kasane-core` `anyhow::Result` uses are scheduled for a follow-up sub-stage that introduces a `Box<dyn Error + Send + Sync>` trait-surface wrapper. All 1892 `kasane-core` lib tests + 176 `kasane` lib tests + workspace integration tests pass; clippy `--workspace --lib -- -D warnings` clean across `default` / `--features gui` / `--features syntax`; `cargo build --no-default-features` green; fmt + doc-consistency clean. |
| δ-4.4a — `anyhow` purge: `kasane-plugin-package` | ✅ 2026-05-14 | New `pub enum FsUtilError { ReadDir, ReadEntry, InspectFileType }` in `kasane-plugin-package/src/fs_util.rs` (each variant pins the offending filesystem path next to `#[source] std::io::Error`); `collect_kpk_paths` / `collect_recursive` switch from `anyhow::Result` + `with_context` chains to `Result<_, FsUtilError>`. `kasane-plugin-package/Cargo.toml` drops `anyhow` (the dep no longer has any consumer). Binary boundary in `kasane/src/plugin_cmd/package_artifact.rs::discover_package_paths` gains one `?` + `Ok(...)` wrap to coerce `Result<_, FsUtilError>` → `anyhow::Result<_>` (via `From<FsUtilError> for anyhow::Error` from `thiserror`); `plugin_store.rs::discover_package_paths` already used `?` so coercion is automatic. Touches: 3 files. All 1892 kasane-core lib tests + 176 kasane lib tests + 6 kasane-plugin-package lib tests + 248 kasane-gui lib tests pass; clippy `--workspace --lib -- -D warnings` clean; `cargo build --no-default-features` green; fmt + doc-consistency clean. First of the 5 sub-stages — the remaining four (δ-4.4b–e) tackle `kasane-tui`, `kasane-gui`, `kasane-wasm`, and `kasane-core` respectively. |
| δ-4.3 — `Result<T, String>` retirement at typed-error boundaries | ✅ 2026-05-14 | 8 functions across 3 crates migrated from ad-hoc `Result<T, String>` to typed `thiserror::Error` enums. **kasane-syntax** (`src/grammar.rs`): new `pub enum GrammarLoadError { LibraryOpen, SymbolMissing }` (each variant carries the originating `libloading::Error` via `#[source]`); `load_language_from_so` returns `Result<Language, GrammarLoadError>`. **kasane-wasm** (`src/convert/input.rs`): new `pub(crate) enum KeyConvertError { InvalidCodepoint }`; the four KIT key-decode helpers (`wit_key_event_to_key_event`, `wit_key_code_to_key`, `wit_key_group_decls_to_compiled_key_map`, `wit_key_pattern_to_key_pattern`) now share `Result<_, KeyConvertError>`; the existing `convert_key_event_rejects_invalid_codepoint` test updated to `err.to_string().contains(...)`. **kasane** (`src/init_cmd.rs`, `src/widget_cmd.rs`, `src/http_manager.rs`): three CLI / runtime command surfaces gained typed errors — `pub enum InitError { AlreadyExists, CreateDir, Write }`, `pub enum WidgetCmdError { Read, Parse, WatcherCreate, WatcherWatch, WatchUnsupported }` (the last two are `#[cfg]`-gated to match the existing `wasm-plugins` feature split for the `notify` watcher path), and (file-local) `enum UrlValidationError { Parse, UnsupportedScheme, Localhost, Loopback, LinkLocal, PrivateNetwork }`. Each variant's `#[error("…")]` string mirrors the prior `format!("…")` output verbatim so plugin-visible error messages (especially the URL-validation strings surfaced through `HttpEvent::Error::error` to plugins) stay byte-for-byte identical. `kasane/Cargo.toml` gains `thiserror = { workspace = true }`. `kasane-syntax/Cargo.toml` gains the same dep. **Justified out-of-scope** (`Result<_, String>` legitimately preserved): the 14 widget-parse helpers in `kasane-core/src/widget/parse.rs` (errors collected verbatim into `WidgetNodeError::message` as the user-facing diagnostic text) and the 3 lint sites in `kasane-plugin-sdk-macros/src/lint.rs` (errors feed directly into `syn::Error::new_spanned(...).into_compile_error()` — the `String` *is* the rendered `compile_error!` payload). Touches: 7 production files + 1 test file + `Cargo.toml` × 2 + roadmap. 1892 `kasane-core` lib tests + 176 `kasane` lib tests + 156 `kasane-wasm` lib tests + 6 `kasane-syntax` lib tests + workspace integration tests all pass; clippy `--workspace --lib -- -D warnings` clean across `default` / `--features gui` / `--features syntax`; `cargo build --no-default-features` (TUI-only) green; fmt + doc-consistency clean. |
| δ-4.2 — Manual `Display + Error` impls → `thiserror::Error` derive | ✅ 2026-05-14 | 10 `kasane-core` library error types migrated from hand-written / missing `std::fmt::Display` + `std::error::Error` impls to `#[derive(thiserror::Error)]` with `#[error("…")]` per variant. **Types touched**: `SessionManagerError` (`session.rs` — gained `Error` impl, previously Debug-only), `HistoryError` (`history/mod.rs` — Debug-only → Error), `SaveError` / `LoadError` (`state/selection_set.rs` — Debug-only → Error), `OwnerValidationError` + `OwnerValidationErrorKind` (`surface/resolve.rs` — added `Display` for the kind enum so the struct's `Error` derive can interpolate it), `SurfaceRegistrationError` (`surface/types.rs` — Debug-only → Error), `WidgetParseError` (`widget/parse.rs` — manual Display → derived), `TemplateParseError` (`widget/template.rs` — manual Display → derived; `ConditionalError(CondParseError)` gained `#[from]` for ergonomic `?`), `CondParseError` (`widget/condition.rs` — manual Display → derived), `ConfigError` (`config/kdl_parser.rs` — manual Display → derived), `UnifiedParseError` (`config/unified.rs` — manual `Display` + manual `impl Error` → derived; `Widget(WidgetParseError)` gained `#[from]`). `WidgetNodeError` left alone — it's a diagnostic record fed into `PluginDiagnostic`, not propagated as an `Err(_)`, so it doesn't need `Error`. **Aggregate extension**: `CoreError` (`kasane-core/src/error/mod.rs`) gained 10 new `#[error(transparent)] #[from]` variants: `ConfigField`, `WidgetParse`, `TemplateParse`, `CondParse`, `Session`, `History`, `SelectionSetSave`, `SelectionSetLoad`, `SurfaceOwnerValidation`, `SurfaceRegistration`. Variant set still `#[non_exhaustive]` (anticipated δ-4.3 / δ-4.4 / δ-4.5 additions for syntax / wasm / binary layers). 12 unit tests in `error::tests` validate `From` conversions and `Display` pass-through. **Display wording preserved**: every variant's `#[error("…")]` reproduces the previous manual `write!()` output verbatim (e.g. `"KDL syntax error: {0}"`, `"condition expression too complex (max 16 nodes)"`); the only intentional rewording is the previously-missing variants where new messages were chosen to mirror Debug shape (`"duplicate session key: {0:?}"`, etc.). Touches: 10 files (8 type-definition sites + `error/mod.rs` extension + `error/mod.rs` 9 new tests). 1868 kasane-core lib tests pass; 1892 lib tests across the wider workspace also pass; clippy `--workspace --lib -- -D warnings` clean across `default` / `--features gui` / `--features syntax`; `cargo build --no-default-features` (TUI-only) green; `cargo fmt --check` + `tools/check-doc-consistency.sh` clean. Zero consumer migration: existing call sites continue to construct / pattern-match each type unchanged; `thiserror::Error` derive is purely additive. |
| δ-5 — Cargo features expansion | ✅ 2026-05-13 | Restated scope: the existing `wasm-plugins` / `gui` / `syntax` flags at the `kasane` top-level crate were the right location (kasane-core has no `kasane-syntax` / `kasane-wasm` deps, so feature flags there would gate nothing); a `std-os` flag in `kasane-core` would fragment the API surface around config + workspace persistence without a clear consumer demand, deferred. **What landed**: fixed the `--no-default-features` (TUI-only, no WASM) build — previously broken. Three sites needed cfg gates: `kasane/src/lib.rs::mod locked_wasm_provider` now `#[cfg(feature = "wasm-plugins")]` (the entire bundled-WASM lock-resolver lives behind the flag); `kasane/src/plugin_cmd/resolve.rs::{append_bundled_fallbacks, should_enable_bundled}` split into `wasm-plugins` and stub-no-op variants so callers don't have to feature-gate (no-op stub returns `Ok(())` since there are no bundled plugins to fall back to without WASM); `ResolveReason::{BundledDefault, BundledEnabled}` variants annotated `#[cfg_attr(not(feature = "wasm-plugins"), allow(dead_code))]` to silence dead-code warnings while preserving exhaustive-match consumers. `kasane/src/orchestrator.rs` imports of `PluginDiagnostic` / `PluginId` moved inside the `wasm-plugins` cfg gate (the no-op `ReloadOrchestrator` impl doesn't need them). `kasane/src/plugin_cmd/dev.rs::Context` import gated to wasm-plugins (only used inside the watch-and-rebuild path that already requires it). **Validated build matrix** (all 5 combos): `cargo build` (default = TUI + WASM); `cargo build --no-default-features` (TUI-only, no WASM); `cargo build --features gui` (TUI + WASM + GPU); `cargo build --features syntax` (TUI + WASM + tree-sitter); `cargo build --features "gui syntax"` (full surface). All pass clippy `-D warnings`. **Documentation**: `docs/getting-started.md` "From Source" section now carries a "Slim-Build Matrix" subsection enumerating the four feature flags (`wasm-plugins`, `gui`, `syntax`, `tui-image`) with default-on/off + adds-what columns, plus 4 representative `cargo build --features …` invocations covering the slim-to-maximum spectrum. **Tests**: `cargo test --no-default-features -p kasane --lib` passes 161 (matches default-features count modulo the wasm-plugins-only tests that don't compile without the feature); `cargo test --lib -p kasane-core` passes all 1928. clippy + fmt clean across 4 feature combos. |
| ε-1.b — `kasane-core` public API contraction | ✅ 2026-05-14 | `kasane-core/src/lib.rs`: `pub mod salsa_db` and `pub mod salsa_sync` gained `#[doc(hidden)]` attribute, bringing them in line with the existing `#[doc(hidden)] pub mod salsa_queries` / `#[doc(hidden)] pub mod salsa_views` pattern. The four salsa_* modules now uniformly hidden from rustdoc; plugin authors browsing docs.rs see `kasane_core::salsa_*` as not-part-of-public-API. `kasane-internal`'s `pub use kasane_core::salsa_{db,queries,sync,views};` re-exports remain valid (rustc does not enforce doc(hidden) at compile time, only rustdoc rendering). Full `pub(crate)` downgrade is structurally blocked by the circular-dep concern documented in ε-1's closure note (kasane-internal's re-export would break, and moving the modules to kasane-internal creates kasane-internal → kasane-core → kasane-internal cycles via the input/query graph). Validation: workspace build + lib tests pass; clippy `--workspace --lib -- -D warnings` clean; doc-consistency clean. Net change: 2 lines (`#[doc(hidden)]` annotations only). |
| ε-1 — `kasane-internal` crate | ✅ 2026-05-13 (façade landed; contraction follow-up landed as ε-1.b 2026-05-14) | New `kasane-internal/` workspace member — a thin façade re-exporting `kasane-core`'s `#[doc(hidden)] pub` surface plus the Salsa runtime crate-graph items: `salsa_db`, `salsa_queries`, `salsa_sync`, `salsa_views` (from `kasane_core`), `display::algebra` (from `kasane_core::display`), `WireFace` (from `kasane_core::protocol`), `RecoveryWitness` and `SafeDisplayDirective` (from `kasane_core::plugin::algebra::*`). Empty `src/lib.rs` plus `Cargo.toml` carrying the rationale: plugin authors depend on `kasane-core` only; internal renderers / benches / Salsa users go through `kasane-internal`. **Migration scope landed**: `kasane-tui` (`src/lib.rs`, `benches/backend.rs`) and `kasane-gui` (`src/app/mod.rs`, `benches/cpu_rendering.rs`) switched their Salsa imports from `kasane_core::salsa_*` to `kasane_internal::salsa_*` (5 import sites total). `kasane-tui/Cargo.toml` and `kasane-gui/Cargo.toml` gained `kasane-internal = { workspace = true }`. Workspace `Cargo.toml` `members` list extended with `"kasane-internal"` and `[workspace.dependencies]` gained the path entry. CLAUDE.md workspace structure table gains a row. **Deferred — contraction phase**: `kasane-core`'s `pub mod salsa_db` / `pub mod salsa_sync` / `#[doc(hidden)] pub mod salsa_queries` / `#[doc(hidden)] pub mod salsa_views` declarations remain as-is. Downgrading them to `pub(crate)` requires (a) migrating remaining `kasane-wasm` consumers (none today — WIT conversion only touches `WireFace`, which is exported via `kasane_core::protocol` and used through `kasane-core::protocol::WireFace` not `kasane-internal::WireFace`), (b) auditing in-tree benches in `kasane-core/benches/` that still use the `pub` paths, and (c) potentially splitting Salsa types across crate boundaries (the Salsa input/query graph is built from `kasane-core` types, so wholesale movement creates a circular dep). The façade is the visible API-shape commitment; the contraction is the follow-up enabled by it. **Validation**: `cargo build --all-targets` green; `cargo test --lib -p kasane-core` (1928 tests / 0.08 s), `kasane-tui` (45 tests), `kasane-gui` (248 tests / 3 ignored) all pass; clippy + fmt clean across `default` / `--features gui`; doc-consistency clean. **Rationale**: pinning the API-split shape now (façade exists, internal consumers route through it) makes the future contraction a mechanical visibility downgrade rather than a parallel design decision. The roadmap entry is marked closed for the façade work; a follow-up "ε-1.b — kasane-core public API contraction" can land in the same Phase ε once the open `kasane-core/benches/*` references are audited. |
| ε-2 — `kasane-protocol` crate split | ✅ 2026-05-13 | New workspace member `kasane-protocol/` (7 files git-mv'd preserving history: `color.rs`, `message.rs`, `parse.rs`, `style.rs`, `tests.rs`, `wire.rs`, `mod.rs` → `lib.rs`). External deps (`serde`, `serde_json`, `simd-json`, `thiserror`, `bitflags`, `compact_str`) declared in fresh `Cargo.toml`; **zero internal `crate::*` references outside protocol-self** (5 self-references in `tests.rs` + `style.rs::tests` + `message.rs` doc-comment retargeted to `crate::style` / `crate::Attributes` / `crate as protocol` / `crate::color` / `crate::parse`). `kasane-core/src/lib.rs` swapped `pub mod protocol` → `pub use kasane_protocol as protocol;` so the 100+ external `kasane_core::protocol::*` consumers across `kasane-tui`, `kasane-gui`, `kasane-wasm`, `kasane-internal`, the binary, benches, integration tests, and docs **need no migration** — the alias preserves every existing path including `kasane_core::protocol::wire::WireFace`. Single visibility adjustment: `Atom::from_wire(WireFace, _)` demoted from `pub(crate)` → `#[doc(hidden)] pub` (the constructor's three callers — `kasane-core/src/test_support.rs`, `state/tests/apply.rs`, `state/derived/tests.rs` — span the new crate boundary; `pub(crate)` would have lost them while `#[doc(hidden)] pub` preserves the "not part of public API" intent through hidden naming). `wire.rs` doc preamble trimmed of historical migration narrative per `.claude/rules/doc-general.md` (the closure-cascade backlog reference removed; canonical path now `kasane_protocol::wire::WireFace`). Validation: `cargo build` workspace clean (12s); `cargo test` 2829 passed / 0 failed across 61 binaries; `cargo clippy --lib` clean for `kasane-protocol`, `kasane-core`, `kasane-tui`, `kasane-wasm`, plus `--features gui` and `--features syntax`; `cargo fmt --check` clean; `tools/check-doc-consistency.sh` green (0 errors, 2 pre-existing warnings; CLAUDE.md workspace check now sees 18 paths). LoC distribution: 2838 protocol LoC moved out of `kasane-core/src/` cleanly (no scatter into other modules); `kasane-core/src/lib.rs` net −1 (`pub mod protocol;` → `pub use kasane_protocol as protocol;`). |
| ε-3 — `kasane-core-tests` crate split | ✅ 2026-05-13 | New `kasane-core-tests` workspace member created at the repository root. `git mv kasane-core/tests kasane-core-tests/tests` moved 30 files (~8600 LoC, plus `support/` helpers and `golden/snapshots/` fixtures) preserving git history. The actual integration-test LoC is ~8600, not 20 754 as the roadmap entry estimated — earlier counts likely included other crates' tests as well; the smaller number does not change the rationale. New crate has an empty `src/lib.rs` and `[lib] path = "src/lib.rs"` (Cargo refuses workspace members with no library *or* binary target; the empty lib lets us stay focused on integration tests under `tests/`). `[dev-dependencies]`: `kasane-core` (workspace), `compact_str`, `proptest`, `serde_json` (the latter required by `latency_budget.rs`). Workspace `Cargo.toml` `members` list extended with `"kasane-core-tests"`. CLAUDE.md workspace structure table gains a row for the new crate citing the ε-3 rationale (fast inner loop). **Inner-loop speed**: `cargo test --lib -p kasane-core` now runs the 1928 unit tests in ~0.09 s — the integration tests that historically dominated the test build have moved off the library-crate's compile dependency graph. **Validation**: `cargo build --all-targets` green; `cargo test -p kasane-core-tests` runs 29 integration test groups, all pass; full workspace `cargo test` shows 57 test result groups, no failures; clippy + fmt clean across configurations; `bash tools/check-doc-consistency.sh` clean (after rewriting one ε-4 closure note that contained illustrative link-shaped strings inside backticks which the doc-link checker mis-interpreted as real links). |
| ε-4 — ADR per-file split | ✅ 2026-05-13 | 50 ADRs split out of `docs/decisions.md` (7162 lines, 295 KB) into per-file records under `docs/decisions/adr-NNN[a]-<slug>.md` (zero-padded NNN, slugified title). Each per-file doc starts with a demoted `# ADR-NNN: Title` H1 (was `## ADR-NNN:` in the monolith). The remaining `docs/decisions.md` is now a 117-line index: original Decision Summary table preserved at the top, followed by a generated 50-row table linking each ADR to its per-file path. **In-page anchor links** in the summary table (18 occurrences whose `#adr-nnn-…` targets moved out of the file) were retargeted to per-file URLs of form `./decisions/adr-NNN-…md`. **Cross-document references** (33 occurrences across `docs/index.md`, `abi-versioning.md`, `performance.md`, `plugin-api.md`, `plugin-development.md`, `roadmap.md`, `migration/0.6-to-0.7.md`) of form `…/decisions.md#adr-NNN-…` were retargeted to `…/decisions/adr-NNN-…md` (relative path adjusted for each doc's depth). **Intra-ADR cross-references** (16 occurrences across 10 files — `adr-009`, `adr-021`, `adr-030`, `adr-031`, `adr-032`, `adr-038`, `adr-039`, `adr-044`, `adr-045`, `adr-049`) of form `#adr-NNN-…` were retargeted to sibling-file URLs of form `./adr-NNN-…md`. One self-reference inside ADR-032's instrumentation-plan bullet was rewritten to descriptive text (linking to oneself is redundant). **`tools/check-doc-consistency.sh::check_decisions_adr_refs`** rewritten to: (a) collect canonical defined-set from `docs/decisions/adr-*.md` H1 lines, (b) cross-check the index table in `docs/decisions.md` against the per-file set (drift between them is now an explicit error), (c) keep the existing per-doc ADR-NNN reference scan against the canonical defined-set. Output line updated to `OK: all ADR references resolve (50 unique IDs across 50 per-file records)`. **Validation**: `bash tools/check-doc-consistency.sh` passes (2 pre-existing warnings unrelated to ε-4); 1928 lib tests still pass; build + clippy + fmt unaffected. **Tooling note**: split was driven by a `/tmp/split_adr.pl` Perl script (Python unavailable in nix shell); kept as one-shot. Future ADRs should be authored directly as `docs/decisions/adr-NNN-<slug>.md` with the `# ADR-NNN: Title` H1 + a corresponding entry in the `decisions.md` index table — the consistency check enforces both. |
